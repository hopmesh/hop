{
  "_comment": [
    "Every secret name .github/workflows/** reads, and the ONE scope it is provisioned in.",
    "tools/workflow-secrets-guard.py holds the workflows to this file statically (in CI) and, when",
    "run with a token that can read the secret inventory, checks this file against the live scopes,",
    "printing names only, never values. secrets.GITHUB_TOKEN is the runner-issued token and is not",
    "listed. A scope of environment:<name> means only a job declaring that environment can read it;",
    "the guard fails when a job reads such a name without declaring the environment, which is the",
    "exact defect that left the ESP32 release path referencing names no scope had ever held.",
    "provisioned:false marks a name that is DECLARED but not set anywhere yet. It is a standing to-do,",
    "not an exemption: the workflow reading it must fail fast NAMING it rather than dying obscurely",
    "downstream, and the guard requires that fail-fast step to exist.",
    "Every name here is asserted set/unset by branch-protection-audit.yml on its weekly run, one job",
    "per scope so each job can read exactly what its scope allows. `--assert-present` compares that",
    "against this file, and `--assert-present` coverage of every declared name is itself checked",
    "statically in CI, so adding a secret here without an assertion is a red build.",
    "GCP deploy, Stripe, Resend and BOOTSTRAP_TFVARS secrets moved to hopmesh/platform along with",
    "the billing-catalog.yml, bootstrap-apply.yml, runtime-deploy.yml and branch-protection-audit.yml",
    "workflows that consumed them; those names are declared in the platform repo's own copy of this file."
  ],
  "secrets": {
    "BRANCH_PROTECTION_TOKEN": {
      "scope": "repository",
      "provisioned": false,
      "purpose": "branch-protection-audit.yml: administration:read, the only way to read main's live protection rule",
      "blocked_on": "repository PAT with administration:read on hopmesh/hop needs to be provisioned as a repository secret by the repository owner"
    },
    "HOP_SYNC_TOKEN": {
      "scope": "organization",
      "purpose": "changelog.yml and pr-automerge.yml: cross-repo contents and pull-request writes",
      "blocked_on": "organization PAT whose replacement by a scoped repository or GitHub App token is owner-held (requires repository owner provisioning a scoped App or repository secret with contents and pull-requests permissions across hopmesh/hop)"
    },
    "MIRROR_SECRET_AUDIT_TOKEN": {
      "scope": "repository",
      "provisioned": false,
      "purpose": "branch-protection-audit.yml: reads secret METADATA (names only) on the hopmesh mirrors, to catch a mirror release workflow referencing a credential nobody seeded",
      "blocked_on": "no such PAT exists yet; it needs 'secrets: read' on the hopmesh mirrors plus admin:org for organization-scoped secrets. Until it is added, the mirror-secret audit job FAILS rather than reporting a green skip, and the equivalent check is `python3 tools/release/check-mirror-secrets.py` run by hand. Deleting that job is the other legitimate resolution."
    },
    "HOP_SYNC_APP_ID": {
      "scope": "environment:component-sync",
      "purpose": "sync-components.yml, release-tags.yml: the mirror-writing GitHub App"
    },
    "HOP_SYNC_APP_PRIVATE_KEY": {
      "scope": "environment:component-sync",
      "purpose": "sync-components.yml, release-tags.yml: the mirror-writing GitHub App"
    },
    "NATIVE_ARTIFACT_SIGNING_KEY": {
      "scope": "environment:release",
      "purpose": "native-artifacts.yml: signs the native release manifest the mirrors verify"
    },
    "CRATES_IO_TOKEN": {
      "scope": "environment:release",
      "provisioned": false,
      "purpose": "crates-publish.yml: publishes hop-mesh-core, hop-mesh-store-sqlite, and hop-mesh-store-firestore to crates.io",
      "blocked_on": "crates.io publishing token for hopmesh organization needs to be provisioned in the release environment by the repository owner"
    }
  }
}
