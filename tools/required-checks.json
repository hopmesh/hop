{
  "_comment": "Canonical CI job set in ci.yml order. tools/check-required-checks.sh keeps this in lockstep; tools/release-provenance.py verifies every release tag ran every check. The CI gate is the one required branch-protection context, and a successful push-main run is the only automatic runtime deploy trigger.",
  "required_checks": [
    "Detect changed areas",
    "Rust (test · clippy · fmt)",
    "Cargo supply-chain deny (cargo-deny)",
    "Kotlin SDK tests (BearerManager)",
    "Compose Multiplatform SDK tests",
    "Android bearers + driver (JVM unit tests)",
    "Apple bearers + driver + app (build-only)",
    "WASM (wasm32 build)",
    "Web + sim (Astro build · scenario-check · link check)",
    "Contract purity + header drift + C smoke",
    "Automation authority guards",
    "Docs token guard (banned copy)",
    "Node endpoint SDK (proofs)",
    "Python endpoint SDK (proofs)",
    "Go endpoint SDK (race)",
    "Ruby endpoint SDK (proofs)",
    "Crystal endpoint SDK (spec)",
    "Elixir endpoint SDK (mix test)",
    "Flutter endpoint SDK (proofs)",
    "Lowest supported dependency bounds (Python + Go)",
    "React Native SDK (typecheck + tests)",
    "Infrastructure validation",
    "CI gate"
  ]
}
