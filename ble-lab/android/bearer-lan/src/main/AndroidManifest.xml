<?xml version="1.0" encoding="utf-8"?>
<!-- Library module: namespace is declared in build.gradle.kts. The INTERNET / ACCESS_NETWORK_STATE
     permissions the LAN sockets need stay in the consuming app's manifest (net.waldrip.blelab) — they
     are NOT declared here. -->
<manifest />
