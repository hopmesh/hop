# ---- Rust ----
/target
# the wasm site is excluded from the workspace, so it has its own target dir
site/target/
**/*.rs.bk
# relay identity seeds + on-disk stores (never commit secrets/state)
*.db
*.db.key

# ---- macOS ----
.DS_Store

# ---- iOS / Xcode ----
# generated XCFramework + Swift bindings (legacy path + the HopDriver package)
apple/generated/
apple/HopDriver/Frameworks/
apple/HopDriver/Sources/HopFFIBindings/hop_ffi.swift
apple/HopDriver/.build-staging/
apple/HopDriver/.build/
# regenerate with xcodegen
*.xcodeproj
# xcodebuild output / DerivedData
apple/HopDemo/build/
**/xcuserdata/

# ---- Android ----
android/HopDemo/generated/
.gradle/
android/HopDemo/.gradle/
android/HopDemo/build/
android/HopDemo/app/build/
android/HopDemo/local.properties

# Local marketing-site preview renders
web/.preview/
apple/HopBearers/.build/

# libhop C smoke-test binary (build artifact; source is examples/smoke.c)
crates/hop-ffi/examples/smoke

# SwiftPM build dirs for the SDK wrappers / bearer packages
sdk/wrappers/Hop/.build/
bearers/apple/**/.build/

# Kotlin/JVM SDK build dirs
sdk/wrappers/kotlin/.gradle/
sdk/wrappers/kotlin/build/

# ESP32 example host binary
apps/esp32/hop-sensor/hop-sensor
