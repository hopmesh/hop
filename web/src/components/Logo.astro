---
// The Hop wordmark — cursive "hop" as a network route (nodes, breaks, arrow → node).
// Inlined so it inherits color (var(--logo)) and flips with the theme.
import raw from '../assets/hop-mark.svg?raw';
---
<span class="hop-logo" set:html={raw}></span>
