# `site/` is retired — see `web/`

This directory was an early **Dioxus** marketing site. It is **not built or deployed** by anything.

The live marketing + docs site is **`web/`** (Astro), published to GitHub Pages by
`.github/workflows/pages.yml` and fronted by the `hopme.sh` domain (`web/public/CNAME`).

Do not add new work here. This dir is kept only to avoid breaking any stale local checkouts; it can
be deleted outright once nothing references it. (GAP-ANALYSIS.md F-38.)
