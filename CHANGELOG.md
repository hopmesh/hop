# Changelog

Notable changes, generated from [conventional commits](https://www.conventionalcommits.org) by
git-cliff. Do not edit by hand.
## Unreleased

### Bug Fixes
- ship our own UTF-8 codec, since Hermes has no TextDecoder (e0a545a)
- describe ABI levels in words so the version sweep stays honest (bbcae1c)
- pin ABI 6, and close the guard hole that hid the drift (5e530fd)
- make wire-version-gated drops visible to operators (e87be44)
- keep published-SDK wire versions deliverable through a current relay (7019ff2)
- restore the AAR metadata floor and run Detox on its real API (e4aa1dd)
- name the real source-app secret and drop a dangling pointer (fd170ae)
- give the release the fleet chain of custody (7f1d882)
- teach the release-provenance test about the CocoaPods publish (1606bc0)
- align Kotlin with the RN 0.87 consumer floor and drop the metadata suppression (164cf00)
- satisfy the executable-reference and docs-dash guards for the RN HopDemo (1c20a3f)
- pin the cargo-ndk install version the guard demanded (36bf65c)
- probe Maven with HEAD, and name the real dev-path script (085e941)
- make the Android module resolvable, and enforce the bridge contract (666ab69)
- stop a vaccine-blind test failing on a 1-in-101 coin toss (64a0032)
- assert allow_auto_merge, the live setting that silently disarmed pr-automerge (73cc4c1)
- format main.go, and drop a rebar3 pin upstream can no longer resolve (137ad19)
- advertise a source repository that resolves (dc61629)
- repoint the build identity and published metadata at hopmesh/hop (b2e6203)
- drop DNSSEC mentions + reframe the hops:// example as a hosted service call (#156) (0738ba2)
- stop the San Francisco flash on load + light-mode legibility (#160) (022fe78)
- make the light theme legible (HUD accent text + map labels) (#152) (56bb8cb)
- Quickstart nav button text was dark, not white (specificity bug) (#151) (2835f5a)
- unreadable white-on-mint primary buttons in light theme (#150) (6eff791)
- re-ingest mailbox pulls via LOCAL_LINK so a >cap backlog isn't dropped-after-delete (#143) (8c3ebd9)
- wordmark draw-on was 'just the dots' (opacity ramp had no easing handles) (#142) (53bbb11)
- well-known reach record was born expired (issued_at=0) (#141) (49ebee1)
- pass-5 audit remediation - DNSSEC name-hijack (CRITICAL) + Node reply UAF (HIGH) (#138) (ace223b)
- use-after-free-safe teardown across go/python/node (+ elixir safety test) (#134) (42020e3)
- close F-18d, HpsRekey fails safe under a mid-arm panic (#104) (707d35b)
- root-cause + close the bearer-lan JaCoCo coverage wobble (F-3) (#103) (2c39287)
- check-pkg-fresh treats an untracked committed pkg file as DRIFT, not a fallback (F-web-1) (#100) (7706132)
- HopStatus.forwardHops is UByte (F-1, the F-9 sibling) + a real reinterpretation test (F-2) (#99) (8226686)
- close two bypasses in the F-7 rate cap (F-18a unbounded map, F-18b pre-auth reset) (#96) (e04b550)
- exercise publish_recv_beacon/send_traced/set_default_lifetime_ms/inbox_debug through real wasm (#92) (157cb35)
- close F-7 - per-node-identity rate cap on the driver's Ev::Data (#93) (a16146e)
- panic-isolate the driver loop (F-2, HIGH) - close an unauthenticated remote-DoS gap (#87) (8920726)
- make the branch-protection audit actually pass (token scope + context format) (#85) (510af1a)
- make hexd byte-safe so a hostile DS-digest can't panic the resolver (2nd-pass audit finding) (#78) (92ec08a)
- cache image decode + downscale picked photos (fixes iPad CPU crash + picker hang) (0597a54)
- HopLink keepalive + liveness watchdog (match iOS) so links don't drop every 15s (76e4ef9)
- a live BLE/Wi-Fi link is 1 hop — drop sticky 'direct', categorize by hops (1f6d966)
- periodically re-arm BLE advertising to self-heal a silently-stopped advertiser (aedd701)
- scan with allowDuplicates so peers (esp. Android) get repeated connect chances (e16e4fe)
- handle didModifyServices — re-read L2CAP PSM after an (Android) peer restarts (4dcc1e3)
- direct = live BT/Wi-Fi link OR <=1-hop advert (no flicker to mesh) (2877fa4)
- connectGatt on main thread; BLE link diagnostics (c2b9c6b)
- BLE GATT lifecycle — close failed connects, allow retry, clear on link drop (8d5b1c1)
- BLE L2CAP PSM is 2 bytes (match iOS), enabling cross-platform direct links (4c61418)
- restore hosted/subscribed channels on restart (16bbdef)
- sort peers by stable address; android uses user-assigned device name (a68ce32)
- channel host now receives members' posts (192de58)
- set node clock before publishing prekey so sessions form (9f559f0)
- stop SIGABRT crash from openL2CAPChannel on bad peripheral state (ace6f51)
- a ServiceResponse purges its request (return-path delete, §29) (ca000b2)
- answer non-WebSocket HTTP requests with 200 (Cloud Run health) (9a6f327)
- transport counts reflect real links; Wi-Fi indicator tracks the radio (8de8322)
- remove peer dialing — handoff-only, so nodes never wake nodes (§28) (dd34723)
- keep old managed cert so the proxy cert-map swap applies cleanly (4d55d11)

### CI
- sdk/node CI as a canonical composite action (shared monorepo <-> standalone repo) (#149) (48399b2)
- gate the six server SDKs (node/python/go/ruby/crystal/elixir) (#136) (f67eefd)
- commit Cargo.lock + GitOps build trigger (1st-gen GitHub) (a823481)

### Chore
- bump the rust-dependencies group across 1 directory with 4 updates (#26) (ebaece7)
- bump com.android.application in /apps/android/HopDemo (5afada6)
- bump com.android.library in /apps/android/HopDemo (ba08def)
- bump com.android.library in /bearers/android (5430669)
- bump the web-dependencies group (6529c93)
- bump koffi in /sdk/node in the node-sdk-dependencies group (8975299)
- bump actions/setup-java in the github-actions group (956d877)
- remove a research document that does not belong in a public repo (d6cb284)
- bump koffi (#21) (7776b5f)
- bind the v6 hps surface in the Node and Crystal wrappers (ea6f186)
- bind the v6 hps surface in the C ABI wrappers the guard enumerates (d33eb3e)
- bump gradle-wrapper in /apps/android/HopDemo (3b6a650)
- bump gradle-wrapper from 9.6.1 to 9.7.1 in /bearers/android (ab30a62)
- bump the web-dependencies group across 1 directory with 2 updates (3bf14c3)
- bump jvm from 2.2.10 to 2.4.10 in /sdk/android (4da7bfa)
- bump com.squareup.okhttp3:okhttp in /apps/android/HopDemo (42b7bf9)
- bump org.json:json in /apps/android/HopDemo (fe0a53c)
- bump com.squareup.okhttp3:mockwebserver (df6998f)
- bump com.squareup.okhttp3:okhttp in /bearers/android (3151df0)
- bump org.json:json in /bearers/android (95ea9b2)
- bump com.squareup.okhttp3:mockwebserver in /bearers/android (199154b)
- retire the guards and secrets whose subjects moved to private repos (29315ea)
- ignore the hop-install build output (1b89726)
- bump the node-sdk-dependencies group across 1 directory with 2 updates (#158) (09ba324)
- bump org.json:json in /apps/android/HopDemo (#107) (3882424)
- bump org.robolectric:robolectric in /apps/android/HopDemo (#110) (bc1b8d3)
- bump androidx.test:core in /apps/android/HopDemo (#109) (4b92a7b)
- bump androidx.test:core in /bearers/android (#113) (b078d54)
- bump androidx.activity:activity-compose (#108) (1561680)
- drop the root license, license per-component (FSL-1.1-ALv2) (#146) (570c680)
- drop the dead root site/ scaffold's gitignore entry (#117) (d485022)
- finish the monorepo layout, kill platform stubs, unify the platform axis (O-1/O-3/O-4/O-5) (#115) (a0550d7)
- organize the monorepo, all apps under apps/<platform>/<App> + a CLAUDE.md at every tree node (#105) (e7cd696)
- bump net.java.dev.jna:jna in /sdk/wrappers/kotlin (#18) (68f43a8)
- bump gradle-wrapper from 8.9 to 9.6.1 in /android/HopDemo (#28) (6e7edb2)
- bump com.google.zxing:core in /android/HopDemo (#29) (fed6beb)
- bump jvm from 2.0.21 to 2.4.0 in /sdk/wrappers/kotlin (#26) (b8cb72d)
- bump net.java.dev.jna:jna in /bearers/android (#32) (99c53b3)
- untrack the Kotlin SDK's machine-local build artifacts (caught by the final re-audit) (#77) (82b0772)
- log GATT-server connection state (BLE cross-platform diagnostics) (137a5bb)
- BLE diagnostics (advertise/scan/accept/PSM-read logging) (46bde03)

### Dependencies
- land the grouped rust-dependencies bump (sha2, ed25519/x25519-dalek, chacha20poly1305, snow, rusqlite, p256, uniffi, tungstenite) (#89) (b390cc7)
- Kotlin 2.4/AGP 9.2.1/Compose BOM 2026.06/okhttp 5.4 toolchain migration (#90) (4d2e3c4)

### Documentation
- regenerate from conventional commits (0ba8f06)
- regenerate from conventional commits (288fb51)
- regenerate from conventional commits (f880b09)
- regenerate from conventional commits (adfd838)
- regenerate from conventional commits (9719166)
- regenerate from conventional commits (b185836)
- regenerate from conventional commits (0c6daf4)
- regenerate from conventional commits (8bd2185)
- record that a maintainer's PR here merges itself, and how to opt out (f962247)
- whether Grit Chat LLC needs its own Cloud Billing account (2e05cfa)
- regenerate from conventional commits (7c9cd96)
- regenerate from conventional commits (c563741)
- document the real Apple pod consumption paths and the mirror's export blockers (bd0853d)
- record the podspec mirror gap, and count thirteen ABI copies (590bc47)
- the corrected podspec URLs still cannot install; say so (17d401e)
- stop pointing integrators at podspec URLs that 404 (1f7663d)
- stop saying the C ABI does not export hps:// (58d24fa)
- regenerate from conventional commits (9b0e086)
- correct token scope to hopmesh/hop (529ae98)
- regenerate from conventional commits (85aa20d)
- regenerate from conventional commits (f174097)
- regenerate from conventional commits (b49b07c)
- regenerate from conventional commits (0b7100d)
- add React Native quickstart and update README/CLAUDE for local consumption (9a57b16)
- regenerate from conventional commits (7eb4bed)
- correct a false claim about rebar3, and name the trap that produced it (720abf7)
- describe the three-repo topology instead of one private monorepo (2da743f)
- marketable README template + brand mark + public-repo catalog (#148) (a168262)
- concrete specs for the three remaining follow-ups (#137) (65a07b8)
- decouple hop-core's vocabulary from the sim (comments only) (#119) (795c4d5)
- pass-18 report - adversarial re-audit found 1 CRITICAL + 3 HIGH in this session's code (#101) (fef6ac0)
- mark all pass-17 findings closed + recover the grades whose dip was fixed (#94) (7fc291c)
- pass-17 report + close the docs/CI-guard findings it found (F-1/F-4/F-6/F-8) (#86) (00b5a80)
- §39 hardening report - same-id forgery class closed (r12-r15), hop-core A (#57) (a61ed28)
- recovery report - GPA slide reversed, 5 security vulns closed (#48) (e041d83)
- pass-4 report with a coverage grade folded in (#38) (425f68a)
- both app cutovers done (compile-verified); crate rename deferred per UniFFI-stays design (c15c275)
- handoff current — both full stacks (SDK->bearers->driver) done; remaining = app UI cutovers (6c18991)
- handoff current — tree migration + iOS xcframework done; remaining = the 2 app cutovers (1fadb4c)
- update handoff — driver done, both renames done, deferred Qs resolved (d3159a3)
- libhop architecture + bearer-isolation status (the morning handoff) (496ec7b)
- §32 — mark hps access modes, app-secret isolation, revocation as implemented (0a89bdd)
- §28 cross-region — per-region local spools + online-only relay epidemic (1581cf4)
- §34 residency — detect for locality, declare for residency (c83acfb)
- §34 device inboxes — recipient-keyed mailboxes with locality migration (22b4753)
- §33 data protection & GDPR — durable store contents, residency, levers (21148ab)

### Features
- guard the published Apple pin, allow a validated last_rev, and stop claiming the bearers mirror exists (122350d)
- bind the relay pool and the hps:// channel surface (633e1b0)
- give the Kotlin SDK the hps:// channel surface (3c9a1ed)
- give the Swift SDK the hps:// channel surface (894d361)
- expose the hps:// pub/sub surface over the C ABI (79b8d52)
- publish bearers/apple as the hop-bearers-apple mirror (7f3f0d4)
- ship the Apple SDK as CocoaPods pods, and make the React Native iOS build work (bcb3796)
- rebuild HopDemo as a React Native app, and prove Android builds (b0e7009)
- add an unsigned dev path to publish the AAR to a local Maven repo (bff9573)
- run this repository's own CI, and repoint every canonical-repo gate at hopmesh/hop (d6f9618)
- expose the endpoint CP quorum setter in all six SDKs (#161) (3ce3c0c)
- phase 3 hold-until-coordinated quorum (CP; never double-process) (#159) (9900a0d)
- phase 2 rendezvous ownership (exactly-once when the owner is up) (#155) (50bd74f)
- cluster bindings across all six SDKs (+ passphrase ABI entry) (#154) (865a687)
- self-clustering endpoints (phase 1 dedup) as a hop-endpoint-core layer over the mesh (#153) (76c420c)
- Copybara mirror for sdk/node (the react-native/relay pattern, not fbshipit) (#147) (78871c5)
- smoother wordmark draw-on, 32 subdivided ribbon pieces (#135) (863babb)
- Crystal endpoint SDK (lib bindings, zero shards) + block/channel surface (#132) (cc58756)
- example parity + in-process dev certs across go/python/node/elixir (#133) (dae0895)
- Ruby endpoint SDK (Fiddle, zero gems) + use-after-free-safe teardown (#131) (25845c2)
- reachable-by-name over WSS + discovery (built-in :ssl, no WS deps) (#130) (8fde9a7)
- reachable-by-name over WSS + /.well-known/hop (pure stdlib, zero deps) (#129) (2b84409)
- reachable-by-name over WSS + /.well-known/hop discovery (#128) (f0a4d27)
- reachable-by-name over WSS + /.well-known/hop discovery (consumes the reach record) (#127) (7875f4f)
- self-certifying reachability records (core + ABI) for DNS-free endpoint discovery (#126) (ef8accd)
- wordmark draw-on animation (parts SVG + Lottie) (#118) (2440d6b)
- Go endpoint SDK via cgo (net/http-shaped, proven) (#124) (b8b7554)
- Python endpoint SDK via ctypes (Flask/FastAPI-shaped, proven) (#123) (a7e8ff0)
- Elixir endpoint SDK via a Rustler NIF (Phoenix/Plug-shaped, proven) (#122) (f63360a)
- embeddable Hop endpoint SDK prototype (receive Hop messages in a Node app) (#120) (79ca856)
- cleaned wordmark — refresh site logo + OG card (b78a007)
- icon set — the Hop bug (cursive h → route arrow → node) (5e0723d)
- brand identity refresh — cursive mark, Poppins, icons + OG (0b8b2b6)
- carry L2CAP PSM in the BLE advert; gentle bursty Android central (0af2dbd)
- tap-to-retry a 'Not sent' message (8b580a7)
- sticky 'direct' — keep a once-direct peer in Nearby while reachable (8e153e0)
- persistence story (deferred sends, invites, channel threads, re-advertise) + direct-by-link categorization + multi-peer BLE (19b6104)
- direct/mesh peer sections + per-peer link-type indicators (iOS parity) (d9657b6)
- Channels stack nav parity (access modes, invites, browse, revocation) (6b03d11)
- Channels stack nav + access modes, invites, browse, revocation (279c29a)
- expose hps access modes, invites, discovery, reach, revocation + app secret (b7e486d)
- hps access modes, invites, discovery, reach, revocation (steps 2-3) (277ec15)
- app-secret key material + directory app-scoping (hps isolation step 1) (28a5e66)
- persist chat history, unread app badge, not-sent state, relay pinning (8e83303)
- multipart messages — text + multiple images as one payload (both platforms) (bac6c4a)
- hdp/hops/HNS architecture — hops:// wiring + hop-endpoint + DESIGN (§30/§31) (421a42a)
- robust delay-tolerant ACKs + DTN-scale lifetimes & retx backoff (§7) (2a62670)
- forward-before-evict custody policy + bigger cloud relay window (§6) (9dbc866)
- transparent carrier-streaming — any message/service auto-chunks (§20) (e73960f)
- drop open-web HTTP fetch; document hops:// origin-gateway model (7ebf513)
- add a contact to the address book by base58 address (8a47e3a)
- handoff logs show msg id+dest; app surfaces messages from unknown senders (1f7ff14)
- clear-relay-queue button + Node.clear_queue / FFI (27ecb1b)
- live network-log stream over HTTP (§ops) (bc54362)
- identify-resolved traces, relays in list, per-link symbols (§29) (10d9617)
- service calls (hop.identify) + per-region subdomains phase 2 (§29) (19fd15a)
- cross-partition handoff — the offline-destination mailbox (§28) (a326bbd)
- device check-in — auto-connect + auto-reconnect to relay.hopme.sh (a1e12e9)
- per-region identity + wire --region/--advertise (§28) (f22a650)
- surface traces+routes in app; stamp carrier app id per hop (311cf82)
- §28 liveness registry + pull-on-wake dialing (3b59cd6)
- §27 provenance traces + learned routes + utility-prioritized epidemic (37a19b2)

### Other
- apply dart format to the new hps bindings (be0dd51)
- publish the three pods to CocoaPods trunk (9dd5b93)
- make the scroll container addressable (f9aed30)
- make the JS bundle resolvable, or the app cannot run at all (a7013d0)
- a real relay bearer, so two devices can exchange a message (cc262dd)
- bootstrap script to create + publish the SDK mirror repos (#163) (0de6d36)
- make the TLS-served reach record the only name path (drop DNSSEC-over-DoH) (#139) (4a9b3a7)
- one consistent endpoint surface across node/python/go/elixir (#125) (b6f9884)
- gate HopDriver coverage from llvm-cov JSON, not a positional awk column (F-CI-7) (#102) (c2f7a42)
- close 5 guard gaps pass-18 found (anchor-job bypass + 4 dash/matrix holes) (#98) (c64dc79)
- verify_publish uses verify_strict, matching the crate-wide ed25519 convention (#95) (6b3da03)
- migrate to Astro 7 (land Dependabot #23) (#88) (76cff92)
- lift HopBearer+Hns.swift 59% -> 99% + add a per-file coverage floor (#83) (8a43911)
- decompose the 1489-line HopBearer god-object into per-concern collaborators (C+ → A) (#76) (5ec9e4a)
- decompose the 1895-line HopBearer god-object into per-concern collaborators (B- → A) (#75) (e2b6589)
- route dedup through the pure keep-rule cores; fix inverted Android dedup-ordering docs (#72) (37d1323)
- close the two capping defects in Endpoint and the Kotlin SDK (#74) (5a629b9)
- root-cause the recurring 0-byte LICENSE bug; version floors; guard (all → A) (#73) (120cb53)
- strip em-dashes from this session's Apple coverage test files (#67) (0e16bf0)
- canonicalize the delivery-vaccine shape at verify (r15-01) — close the is_ack twin on the vaccine id (#56) (d0e67cd)
- bind the entire private inner into the wire id (r14-01) — close the flags.request_ack twin residual (#55) (3977358)
- rebuild wasm pkg for wire v5 (r13 header-bound private id) (#54) (0f1ba6b)
- bind the §39 recognition header into the private wire id (r13-01) — close the 7th chimera vector at its root (#52) (777ec7a)
- cargo fmt the r12 chimera test (rustfmt line-wrap) (#51) (5805444)
- close the §39 recognition-header chimera (r12-01) — the 6th vector (#50) (bcb689c)
- authorize the response-cleanup purges (r11-01) - close the last purge vector (#47) (93c5449)
- enforce the §39 private-bundle invariant at the gate (r10-01 root fix) (#46) (ffcbd7d)
- close the traced-ACK forgery on the DEFAULT private send path (r9-01) (#45) (723cee1)
- close the private-chimera bypass of the traced-ACK authorization (r8-01) (#44) (9790c27)
- restore LICENSE.md (FSL-1.1-ALv2) - the reaudit-close restore did not land (#35) (d8967a9)
- fix Android dial-backoff wedge caught by the hardware gauntlet (#16) (2ce3497)
- refresh GATT cache on dial timeout to break the discovery wedge (bc049b2)
- relay-queue label → "broadcast", fix iOS endpoints misclassification + Android jumbled service log (bbbe1a3)
- remove Destination::InternetEgress (mesh-visible internet-bound leak) (4405bab)
- app identifiers + code packages net.waldrip.* / co.hopmesh.* -> sh.hopme.* (5e5e1a4)
- JNA compileOnly to avoid jar+aar duplicate-class clash (acee770)
- fix the stale F0900BEA beacon UUID -> 7ED7BEAC in the Android driver (09078f5)
- split into HopContract (no libhop) + Hop (libhop node) — unblocks the app cutover (5853f35)
- thin HopDriver composing the SDK + all four bearers (mirror of Apple) (713208d)
- re-home all four bearers as independent modules on the Kotlin SDK (9c09f79)
- ship libhop as an xcframework binary target (device-buildable stack) (83cc2cf)
- thin HopDriver composing the SDK + all four bearers (0bbc57b)
- rename the cdylib/staticlib hop_ffi -> hop (the C ABI is `libhop`) (dc51de5)
- rename sdk/wrappers/swift -> sdk/wrappers/Hop (clean SwiftPM package id) (4ccdaf7)
- contract-purity guardrail — no transport symbols in the contract/SDK (92bdeba)
- bearer kit at parity with Swift (Bearer/LinkSink/BearerManager/HopRuntime) (88c8436)
- a full Hop client in pure C over hop.h (fad73a9)
- idiomatic Kotlin/JVM wrapper over hop.h (JNA) + smoke (3736285)
- add the hops:// request/response surface to the C ABI (1526b34)
- RuntimeSmoke — prove HopRuntime + a Bearer drive the node end to end (84cc85b)
- re-home all four bearers as independent packages on the Hop SDK (48ade1f)
- ship the bearer kit with Hop (Bearer/LinkSink, BearerManager, HopRuntime) (26e61cf)
- idiomatic Swift wrapper over hop.h (Hop) + macOS smoke (665716a)
- expand the C ABI into a complete client SDK (6eeb55f)
- a cbindgen C ABI for hop-core — the universal client + bearer contract (fd2667a)
- emit a timestamp `expireAt` so the TTL policy actually sweeps (61bd6a3)
- add `relaymac` — a headless RELAY-ONLY macOS test client (da261e0)
- request an OAuth scope for the metadata-server token (fix relay 401) (9cb5ab1)
- add Role.relayOnly — relay and nothing else (no BLE/LAN/Wi-Fi) (1702125)
- guard UNUserNotificationCenter for iOS only (macOS CLI crash) (18ab046)
- make untraceable messaging the DEFAULT + private delivery-ACK (b4f5ba0)
- tag shared-bearer peers by real transport in the prod UI (380c858)
- cloud relay as its own lib (both platforms) + re-enable the relay (e696c5e)
- Wi-Fi Direct as its own lib + register in clean-room & prod (72e125e)
- add .gitignore, untrack 1074 committed build artifacts (0921a18)
- fold the shared HopBearers libs into the prod path (App→hop core→bearers) (619a451)
- BearerManager(baseLinkId:) to offset the global link-id space (55efe93)
- LAN bearer as its own lib (Bonjour + TCP) + thread-safe manager (a7bbb70)
- split into core + one lib per bearer (no master library) (e8d00c0)
- add .gitignore, untrack committed build/.gradle artifacts (1f03de4)
- README — the one-interface contract + how to write/register a bearer (915d8f0)
- deterministic registry tests (radio-free proof of multiplexing) (f71c552)
- transport-agnostic Bearer registry (BearerManager) + BLE extraction (ab777ef)
- shared bearer-library keystone — LinkSink/Bearer contract (5364419)
- NSLog alongside print so the iOS device log is capturable for L2CAP-teardown diagnosis (8b46756)
- fix BLE redial storm (prefix-less peers dialed every advert) + bug-hunt findings (3face95)
- clean-room validation results + 4-device harness config (3941599)
- fix multi-peer flood + securing/delivery; P2P test mode + harness (c0da3eb)
- stop the re-gossip directory re-flood (multi-peer resource exhaustion fix) (7068620)
- prove duplicate-link handling is sound; add BLE handshake-flow diagnostics (b6b78e7)
- pure-L2CAP with advert-carried PSM + platform-biased roles (Ditto pattern) (3782c38)
- one-pipe-per-pair gate, immediate fallback, skip L2CAP to Android (4997dd6)
- always discover GATT services so the GATT-data fallback can find DATA (0bc49d1)
- flush deferred ("Securing") content when a session forms, not just on tick (5a8384b)
- auto-heal a wedged BLE stack when Bluetooth cycles (dae6542)
- re-gossip adverts over stable links so prekeys propagate without a reconnect (90e9774)
- keyboard-open layout + offline label (2226017)
- fix frozen keyboard — drive SendingIndicator pulse from the 1s tick (6f3dd69)
- broadcast iBeacon to wake backgrounded/killed iPhones (cross-platform wake) (0ea70dd)
- GATT-first by default, Android-only filter, 0xFFFF-mfg PSM guard, backoff reconnect (78670b8)
- run BLE pure-peripheral — central scan was starving discovery (622f3ea)
- require ratcheting for device-to-device content (fix lockless sends) (eade431)
- diagnostics parity — service log, relay queue, hop traces, identify (4864434)
- unified 4-tab information architecture across iOS + Android (a5b4dd4)
- hops:// WebView browser over the mesh — full iOS parity (3070963)
- hps:// pub/sub, hops:// fetch, HNS — feature parity with iOS (2c33b50)
- image send + display (feature parity, step 1) (3147fbb)
- auto-connect + auto-reconnect the cloud relay (delivery parity) (bd0e5a2)
- // pub/sub phase 3: FFI surface + iOS channels/services UI (b0fb514)
- regenerated Info.plist — BGProcessing task id permitted (4c1f9c8)
- reconnect the relay on background wake + BGProcessing backlog drain (0098461)
- online-only relay-to-relay epidemic (gated, off by default) — §28 phase 1 (bf42f0e)
- keep relay custody of request_ack bundles until the delivery ACK (7551d99)
- persist carrier reassembly so large transfers survive background wakes (889053c)
- track chunked-message delivery status under the original id (a48a375)
- add HopWebView.swift to project.yml sources (980ca87)
- SessionReset — recipient heals a desynced ratchet (ef281b6)
- persist forward-secret ratchet sessions across restarts (d59796a)
- link-layer fragmentation for oversized records — fix silent drop (7a8c89d)
- // pub/sub phase 2: node wire flow over Broadcast (877bf84)
- hps:// pub/sub crypto primitives — phase 1 (DESIGN §32) (46eec10)
- DNSSEC ECDSA P-256 (alg 13) verification (67823c0)
- carry the DNSSEC proof in HnsAnswer — trustless multi-hop (phase 3) (f6f3b62)
- enforce DNSSEC end-to-end — host fetches chain, core validates (phase 2c + 4) (86db0cd)
- validate_and_extract — one-shot trustless resolution (phase 2c) (be1c6dd)
- DNSSEC chain assembler from DoH responses — phase 2b (d885484)
- DoH JSON parsing — phase 2a (5bace5c)
- DNSSEC chain walk to the root anchor — phase 1b (cd0e951)
- DNSSEC validation primitives (RSA/SHA-256) — phase 1 (4a1ae64)
- decentralized multi-hop DNSSEC HNS + §32 hps:// pub/sub (77891a4)
- delay-tolerant resolution + utf-8 error bodies (9162714)
- fix dial_relay deadlock (non-blocking socket) (acacb16)
- a no-internet node resolves by broadcasting to connected peers (c13baf5)
- prune expired records so they leave the device (fec9fd2)
- routable leaf on the mesh + app labels endpoint by its domain (4471594)
- list hops:// endpoints separately, not as relays (0898ec0)
- render scheme-honestly (hops:// vs https://) (5f05110)
- // WebView + content-type plumbing (f7100a9)
- dial the hops:// endpoint directly so requests actually deliver (1c85ef1)
- also serve standard HTTPS (reverse-proxy to origin) (dd79df1)
- only infra relays stamp a trace app id; devices stay fabric (55a5326)

### Performance
- BLE scan BALANCED not LOW_LATENCY — free radio time for the peripheral role (db6719b)

### Refactor
- make hop the source of truth and retire the public/private split (c8b02aa)
- enforce purpose/platform/package (collapse sdk/wrappers, apps/web -> apps/web/site) (#116) (48ec524)

### Testing
- self-test the Hermes globals guard, and run both guards' self-tests (9365f1b)
- prove a bare C consumer can reach a channel, and name ABI 6 in the docs (fe5079e)
- Cucumber and Detox coverage for the React Native HopDemo, split by what one device can prove (da8c10c)
- close store.rs and hps.rs coverage gaps to 100 percent (#81) (f321ed4)
- extract DemoFormat + 21 tests, testable surface 0% → 100% + CI gate (#71) (2c4cc42)
- extract HopDemo pure logic into HopDemoKit + 100% headless coverage gate (#70) (1b66318)
- seam refactor takes BleBearer 7% → 97% (CB-free cores), replace shadow tests (#69) (a20143b)
- LAN 95% / Relay 100% / BLE 99% + fix non-restartable LanBearer (#68) (fff71d4)
- raise host-testable coverage to A (99%) by splitting off the wasm-only surface (#66) (e26c5bd)
- Robolectric suite takes the driver from D (~16%) to A (94% line) (#65) (1baf851)
- headless-node suite raises HopBearer.swift 12.8% -> 88.5% (F -> A) + CI coverage floor (#64) (bbaf239)
- real loopback integration tests for LAN + Relay bearers to >=80% coverage, CI gating, compile-bug root cause (#63) (dadfe5a)
- raise hop-relayd line coverage 41.2% -> 81.1% (#62) (7f7ef1a)
- cover the JNA surface against real libhop to 99.5% (F → A) + CI coverage gate (#60) (01d78f3)
- raise line coverage 39.4% -> 80.9% (#61) (799d798)
- cover the FFI/UniFFI HopNode surface + free helpers (crate 56.5% -> 96%) (#59) (4976545)
- cover the REST layer to 96.7% (from 40.5%) (#58) (2817445)
- refresh fleet address map (sh.hopme.demo identities) (92f303b)
- space Android deep-link sends to avoid LAUNCH_MULTIPLE drops (c7168a0)
- re-add bush + Decklan's iPad to the 6-device fleet (da48589)
- add bush + ipad to the address map; vscode hopmac debug config (2df78bc)
- exchange prekeys in ffi + ws round-trip tests (require-ratchet) (7568e58)
- out-of-order SessionMessages decrypt through the node accessor (9b89c8d)

