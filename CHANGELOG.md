# Changelog

Notable changes, generated from [conventional commits](https://www.conventionalcommits.org) by
git-cliff. Do not edit by hand.
## Unreleased

### Bug Fixes
- let a coordinated version bump pass CI (b714f8b)
- make the PLAT-001 tests actually run, and correct what the last commit claimed (15065c8)
- disabling a transport now actually stops it (PLAT-001) (7b84c71)
- name the unseeded credential that has blocked every publish (5d09ea3)
- `git add -A` committed the swapped Apple manifest, and add the guard for it (228a20d)
- kill the LAN reap-test flake by pinning the ordering (a16a715)
- fmt drift, orphaned doc comments, and Android tests that raced the network (dc44ef4)
- two bugs in my own one-time-prekey work, plus three CI breaks (93859f8)
- require explicit opt-in for a mirror's first ever tag (df25c27)
- finish export_pr (baseline) + import (API auth, reversibility) (912d7cd)
- export_pr reversible_check=False (one-way export transforms) (7ea3a27)
- export_pr baseline via base branch (baseline_from_branch) (093931a)
- authenticate copybara's GitHub API client for export_pr (e679d57)
- surface source_ref as a select output so export_pr gets the PR ref (9b80596)
- hand export_pr the PR ref as copybara's trailing positional arg (26ad068)
- pass the PR ref as COPYBARA_SOURCEREF; drop the leaked probe (31cec8a)
- pass the source PR number to export_pr (bee18b1)
- land logins in the app; center the dashboard content (7023e3f)
- grant the export token workflows write for mirror .github (8cfd729)
- authenticate git via url.insteadOf, drop the credential store (58551dd)
- make the mounted credential files readable in the container (08279f4)
- load git credentials via the system config, not global (48599c4)
- make git read the credential config inside the container (08c3d30)
- stop the per-peer limiter from throttling all logins at once (e3a4e39)
- route the GitHub OAuth callback through the /api proxy (0b9b103)
- a collector refusing every batch fails its health probe (a127c99)
- revive dead ingest, fail closed, bound stamps, measure bytes and carriage (1912c75)
- proxy /api at runtime, not via a build-time rewrite (6b5f2af)
- grant hop-deploy compute.regions.list (dcb33a4)
- billing catalog needs version enable + get, not just add (b346f8c)
- pin a node base digest that actually exists (5cc0962)
- unblock the first console apply (SQL edition + version enable) (271b16c)
- exclude .terraform build artifacts at any depth (3de9683)
- close the IAM drift the first real deploy exposed (611d70d)
- bind the IMMUTABLE OIDC subject GitHub actually issues (e0cf565)
- drop the relay-seed deny policy GCP will not let the applier manage (735e0cc)
- removed{} blocks so the moved admin-plane is forgotten, not destroyed (abea8c5)
- grant bootstrap-apply setIamPolicy on the state bucket (e39c232)
- integrate the storage dimension onto current main (aedcfb2)
- seed the custom secrets role so the first apply is not a 403 race (f1ccec5)
- point the magic-link email at /auth/verify, the page that exists (b230638)
- make the last-Owner floor atomic (team review) (52b3804)
- make first-checkout idempotent + guard against double-subscribe (8d7056e)
- refuse a partially-malformed Stripe price catalog (c1e64ea)
- pin the correct Gradle 9.6.1 distribution SHA-256 (2dbc987)
- address adversarial review of the auth wiring (3 DoS findings) (c90f64e)
- address adversarial review of the auth API (8a48a4d)
- address adversarial review of the auth foundation (73ae5a9)
- bump corpus + all wire-version consumers to v10 (e68d804)
- block SSRF to internal/metadata targets by resolved IP (ADV18-10) (95d51a7)
- coarsen private created_at to defeat a sender timing fingerprint (ADV18-08) (c80f342)
- bind a response to the endpoint we asked, not just the id (ADV18-07) (ea39ccd)
- guard fixed-32-byte C-ABI reads in all wrappers (ADV18-06) (c95c826)
- reap slowloris on raw-TCP and the WS handshake (ADV18-04/05) (4aab3fa)
- forged SessionInit cannot destroy an established ratchet (ADV18-01) (45bc983)
- keep the OTLP failure log aggregate-only + panic-isolate the worker (5c865d0)
- register in-process responder before dialer (efe2b4d)
- history must never gate revenue, and never double-count (cbf43b6)
- close the invoice-existence oracle and add a head-read deadline (a3b8ce5)
- equal-height pricing cards + the missing space before the license (c0fb3a0)
- stamp before fragmentation so streamed bundles stay attributable (b277676)
- strip em-dashes from meter display names + catalog comments (repo no-dash law) (5056592)
- drop snow's unused ring pull; simplify ESP producer (cffcfdd)
- run idf-rust container as root (checkout EACCES), re-point HOME/PATH to esp toolchain (18563df)
- build in idf-rust container (ESP GCC toolchains for ring); split publish (6b58acb)
- add -Zbuild-std for the esp-espidf targets (no prebuilt core/std) (de0e976)
- token via url.insteadOf (+ HOP_SYNC_TOKEN), no credential store (5b2edbd)
- drive the wrapper via its env-var contract (3d4dd70)
- explicit 'migrate' subcommand so the workflow name is parsed (9cc0522)
- register components via a comprehension (no top-level for loop) (b52b1a7)
- run from the config dir with the bare filename (c5bab5d)
- drop redundant 'copybara' arg (image entrypoint already is copybara) (46c5409)
- run Copybara over HTTPS with the token, drop the ssh_key requirement (1782456)
- retain mailbox custody until durable ingest (a16c38e)
- close adversarial protocol gaps (cad3deb)
- drop DNSSEC mentions + reframe the hops:// example as a hosted service call (#156) (33b8643)
- stop the San Francisco flash on load + light-mode legibility (#160) (9237af7)
- make the light theme legible (HUD accent text + map labels) (#152) (abc3059)
- Quickstart nav button text was dark, not white (specificity bug) (#151) (3b2975a)
- unreadable white-on-mint primary buttons in light theme (#150) (65179e6)
- re-ingest mailbox pulls via LOCAL_LINK so a >cap backlog isn't dropped-after-delete (#143) (ea4c235)
- wordmark draw-on was 'just the dots' (opacity ramp had no easing handles) (#142) (2984654)
- well-known reach record was born expired (issued_at=0) (#141) (e0fb4db)
- pass-5 audit remediation - DNSSEC name-hijack (CRITICAL) + Node reply UAF (HIGH) (#138) (d207acc)
- use-after-free-safe teardown across go/python/node (+ elixir safety test) (#134) (42a4a2e)
- close F-18d, HpsRekey fails safe under a mid-arm panic (#104) (879019b)
- root-cause + close the bearer-lan JaCoCo coverage wobble (F-3) (#103) (5c2045f)
- check-pkg-fresh treats an untracked committed pkg file as DRIFT, not a fallback (F-web-1) (#100) (ce3c19e)
- HopStatus.forwardHops is UByte (F-1, the F-9 sibling) + a real reinterpretation test (F-2) (#99) (e4169b2)
- repair the branch-protection audit that PR #85 broke (invalid permissions scope) [CRITICAL] (#97) (1073f04)
- close two bypasses in the F-7 rate cap (F-18a unbounded map, F-18b pre-auth reset) (#96) (c07850c)
- exercise publish_recv_beacon/send_traced/set_default_lifetime_ms/inbox_debug through real wasm (#92) (00c8534)
- close F-7 - per-node-identity rate cap on the driver's Ev::Data (#93) (9f1f743)
- panic-isolate the driver loop (F-2, HIGH) - close an unauthenticated remote-DoS gap (#87) (ac74b79)
- make the branch-protection audit actually pass (token scope + context format) (#85) (bf20fd8)
- authenticate the require-ci deploy gate for the PRIVATE repo (3rd deploy blocker) (#84) (d948c81)
- unbreak the GitOps deploy - it was doubly broken and red for 40+ builds (#82) (0048002)
- make hexd byte-safe so a hostile DS-digest can't panic the resolver (2nd-pass audit finding) (#78) (b75aef7)
- cover Destination::Vaccine in every workspace crate (relay/relayd/hop-sim) + workspace fmt/clippy (e611c4d)
- fitBounds pads for the actual overlays so both stage ends stay visible (0d781f4)
- dense contact chains everywhere, honest congestion, per-device bubble truth, real-street clockin (19b3580)
- stop gradient arrows to the removed relay; counter = message copies (breathes); smaller park/plaza AP (84a44fe)
- stop the narrator re-render flicker; brand-color + larger Hop logo (8bc1e87)
- draw the AP router icon directly — stop depending on the unreliable Pro fetch (1db7683)
- Wi-Fi AP radii down to true real-world ranges (22-70m) (2a99aab)
- lift the store readout above the legend (was overlapping) (ba5a795)
- default to in-memory store (reliable boot); OPFS opt-in via ?opfs (13c106c)
- time out OPFS init so a stale-tab lock can't hang 'booting real nodes' (9eb04bc)
- per-device delivery truth — sender learns delivery only via ACK (5868328)
- fresh OPFS pool name so the stale 64-handle pool isn't reused (47479a7)
- one shared OPFS SQLite DB (namespaced by node), not one-per-node (e785bdd)
- conversation windows to screen edges + hide carrying msgs from recipient (ae89e4c)
- prune old bundles (real OOM fix) + conversation view + compose close (e2f0592)
- only visualize the message's own bundle (kills persistent gossip lines) + ack double-check (0e8ce8e)
- bound memory (stop OOM) + persistent per-bundle flood + tile fix (8cd01a2)
- straight node-to-node lines (no bowed off-node vertices) (70e0d45)
- show delivered lines (backbone deliveries + brighter relay web) (bb7b7a1)
- keep delivered paths, no expiry, bound memory (stop the OOM) (68bd619)
- declutter relay legs + warm up prekeys so cell-off has mesh activity (76e55f4)
- relayd F-18 test used the pre-F-06 mailbox_tag signature (firestore feature) (967a9a9)
- correct industrial image scale; route CTAs to the early-access form (b1a0201)
- on-brand social card + favicon/icons (Signal green) (3b4dd72)
- cache image decode + downscale picked photos (fixes iPad CPU crash + picker hang) (95a12a2)
- HopLink keepalive + liveness watchdog (match iOS) so links don't drop every 15s (a31305d)
- a live BLE/Wi-Fi link is 1 hop — drop sticky 'direct', categorize by hops (3dd93bc)
- periodically re-arm BLE advertising to self-heal a silently-stopped advertiser (08c7a44)
- scan with allowDuplicates so peers (esp. Android) get repeated connect chances (9a6eb15)
- handle didModifyServices — re-read L2CAP PSM after an (Android) peer restarts (d4a9a7e)
- direct = live BT/Wi-Fi link OR <=1-hop advert (no flicker to mesh) (ba6f5e7)
- connectGatt on main thread; BLE link diagnostics (8f80076)
- BLE GATT lifecycle — close failed connects, allow retry, clear on link drop (1316866)
- BLE L2CAP PSM is 2 bytes (match iOS), enabling cross-platform direct links (01d6e34)
- restore hosted/subscribed channels on restart (4a5d619)
- sort peers by stable address; android uses user-assigned device name (e43378b)
- channel host now receives members' posts (009b32f)
- set node clock before publishing prekey so sessions form (d86d2aa)
- stop SIGABRT crash from openL2CAPChannel on bad peripheral state (deacde5)
- a ServiceResponse purges its request (return-path delete, §29) (074f54e)
- answer non-WebSocket HTTP requests with 200 (Cloud Run health) (f6719f0)
- transport counts reflect real links; Wi-Fi indicator tracks the radio (ba24217)
- remove peer dialing — handoff-only, so nodes never wake nodes (§28) (dd78ed7)
- recreate https proxy on the cert map (in-place swap 412s) (d6e9130)
- keep old managed cert so the proxy cert-map swap applies cleanly (b6964c0)
- gate Certificate Manager resources on API enablement (phase 1 retry) (f8b884c)

### Build
- Dockerfile + Next standalone output for Cloud Run (P4a) (392abbe)
- bump the web-dependencies group across 1 directory with 2 updates (ad0713e)
- Dockerfiles for hop-billingd and hop-accountd (20d6d81)

### CI
- stop a disarmed audit being permanently red, and a test using a timeout as control flow (8be22a0)
- name the third tracked tree the local mirror rewrites (0833e7f)
- make the local mirror honest and delete the ESP release path it could never run (907520b)
- close the release-engineering findings from the adversarial audit (aa04a89)
- stop the wire-corpus step name pinning a version it cannot keep current (c7aa7ef)
- stop the rust job exhausting the runner's disk (ffb04bc)
- run the security surfaces that were never compiled, and close the ABI guard hole (59392bf)
- name the wire-corpus step for the version it actually checks (f7e0e18)
- open a PR instead of pushing to protected main (9ef8509)
- docs-token-guard also sweeps the source trees for dashes (aef5a73)
- expect the bumped token action in every release workflow (2c0ecf2)
- bump create-github-app-token to v3.2.0 across all mirrored components (efc9f6c)
- sync the app-token guard pin to the bumped create-github-app-token (bf317e5)
- auto-merge all non-draft PRs + tag Claude on failing dep PRs (5981f52)
- fail deploy gate closed on empty allowlist + harden ESP release input (ADV18-11) (6dd3c2c)
- drop unused attestation read access (0137b19)
- libhop ESP32 prebuilt producer (unblocks PlatformIO) (61cbf30)
- re-test apple on cirunner with SessionCreate in the daemon (8450398)
- re-trigger apple on cirunner after HOME/cargo runner fixes (08051f6)
- run apple on the isolated cirunner self-hosted runner (2a1ff68)
- revert apple to GitHub-hosted (self-host mutated the user keychain) (c61da96)
- self-host the apple job with a CI keychain for the Keychain/SE tests (f081f59)
- fix the full path-filter matching everything (negation bug) (067f6cc)
- grant pull-requests:read to the changes job (989a45c)
- path-filter the heavy jobs behind the CI gate (phase 2) (e9fccc3)
- add the aggregate CI gate job (phase 1 of path-filtered CI) (a9174b2)
- per-repo release workflows (publish on a vX.Y.Z tag) (277cf32)
- auto-merge Dependabot PRs on green (squash) (#157) (e880f0b)
- sdk/node CI as a canonical composite action (shared monorepo <-> standalone repo) (#149) (85d885b)
- gate the six server SDKs (node/python/go/ruby/crystal/elixir) (#136) (128d003)
- trigger the Pages deploy on sim/** changes too (1769f84)
- exercise the sqlcipher (at-rest encryption) feature build+test (F-25) (6a04586)
- before_apply hook to wait for the commit's image before deploying (5ffff24)
- build on every push + activate SHA-tracking deploy (b7b446d)
- commit Cargo.lock + GitOps build trigger (1st-gen GitHub) (44a55f6)

### Chore
- ignore Kotlin compile-daemon scratch in every gradle tree, not just sdk/android (27bce18)
- drop two build artifacts a git add -A swept into the previous commit (4f1f1d3)
- bump com.android.application to 9.3.1 to match the library plugin (dc777e1)
- bump the github-actions group across 1 directory with 3 updates (1925d5e)
- bump astro (bced08b)
- bump org.json:json in /bearers/android (f1b4e0a)
- bump com.android.library in /apps/android/HopDemo (045ff66)
- bump org.json:json in /apps/android/HopDemo (49b7d48)
- bump koffi in /sdk/node in the node-sdk-dependencies group (9e3f7c8)
- bump com.android.library in /bearers/android (65ed034)
- re-trigger CI (62103c6)
- purge em-dashes and en-dashes from source (d222435)
- gate ssrf ip filter behind reqwest feature; fmt (a6f4665)
- rebuild sim/pkg for wire v8 (carriage stamps) (1eed379)
- rebuild sim/pkg after dropping ring (removes ring wasm symbol) (82716bf)
- bump the node-sdk-dependencies group across 1 directory with 2 updates (#158) (1af6155)
- bump org.json:json in /apps/android/HopDemo (#107) (2a893c5)
- bump org.robolectric:robolectric in /bearers/android (#114) (dfb51ce)
- bump org.robolectric:robolectric in /apps/android/HopDemo (#110) (89cb246)
- bump androidx.test:core in /apps/android/HopDemo (#109) (096fe25)
- bump androidx.test:core in /bearers/android (#113) (df1e0e0)
- bump androidx.activity:activity-compose (#108) (1c25e4b)
- drop the root license, license per-component (FSL-1.1-ALv2) (#146) (be2a5a7)
- drop the dead root site/ scaffold's gitignore entry (#117) (9b178d3)
- finish the monorepo layout, kill platform stubs, unify the platform axis (O-1/O-3/O-4/O-5) (#115) (b56bb49)
- organize the monorepo, all apps under apps/<platform>/<App> + a CLAUDE.md at every tree node (#105) (73c3249)
- bump net.java.dev.jna:jna in /sdk/wrappers/kotlin (#18) (7177774)
- bump gradle-wrapper from 8.9 to 9.6.1 in /android/HopDemo (#28) (ceb5396)
- bump com.google.zxing:core in /android/HopDemo (#29) (ea532f8)
- bump jvm from 2.0.21 to 2.4.0 in /sdk/wrappers/kotlin (#26) (9e74ee2)
- bump net.java.dev.jna:jna in /bearers/android (#32) (257bdea)
- bump the github-actions group across 1 directory with 10 updates (#19) (dbb3f85)
- untrack the Kotlin SDK's machine-local build artifacts (caught by the final re-audit) (#77) (47aaf46)
- log GATT-server connection state (BLE cross-platform diagnostics) (6d29842)
- BLE diagnostics (advertise/scan/accept/PSM-read logging) (9c7d965)

### Dependencies
- AGP 9.3 + Gradle 9.6.1 + Kotlin 2.4.10 + core-ktx 1.19.0 (13 held bumps) (471edb7)
- bump the pinned action versions (github-actions group) (86a2138)
- land the grouped rust-dependencies bump (sha2, ed25519/x25519-dalek, chacha20poly1305, snow, rusqlite, p256, uniffi, tungstenite) (#89) (2038ce9)
- Kotlin 2.4/AGP 9.2.1/Compose BOM 2026.06/okhttp 5.4 toolchain migration (#90) (d4844bd)

### Documentation
- the deferred-teardown note was itself an overclaim (bc86421)
- name where each anonymity caveat is mirrored in source, correctly (3f99766)
- restate the gradient cap in its real unit, correct the 1-byte prefix figures, record two open residuals (a99af07)
- record the deferred CLAIM-004 width edits where a reader will hit them (459a112)
- correct claims stated more strongly than the code supports (CLAIM-001..009) (f3fbfbf)
- record the dash violation I put in a commit message, and why it stands (75098fc)
- pin the exact v14 corpus deltas instead of rounding them to 'all' (4cb8d8e)
- record the three mint-step failure signatures (3bf0a06)
- regenerate from conventional commits (b96e019)
- tools/CLAUDE.md still listed the retired manifest exclusion (3138ea5)
- regenerate from conventional commits (330c8c6)
- regenerate from conventional commits (096180b)
- regenerate from conventional commits (102ae67)
- the reorg left three more build recipes pointing at paths that moved (95c7bb5)
- the HopDemo README documented a build that cannot work (7278753)
- node.rs called shipped fragmentation a TODO, and pointed at the dead layer (45f9b6f)
- stop describing a routing algorithm the code no longer runs (5433b6e)
- regenerate from conventional commits (1572ae2)
- make §6 match the shipped router, rename the fossil (49f2f53)
- regenerate from conventional commits (a355901)
- archive the 2026-07-09 ground-up audit report (9e04e8b)
- rewrite deploy docs to GitHub-is-change-management; canonicalize required-checks (cd2892e)
- de-dash relay-connection-policy doc (repo no-dash law) + sync to main (0456453)
- record ADV18 remediation status + correct the unsound ADV18-03 fix (086a864)
- archive July 15 adversarial baseline (5510cd9)
- add remediation process forensic assessment (22f8b13)
- drop stale per-bundle refs (reach-based is the model) (862f80f)
- strip pre-existing em-dashes (repo no-dash law) (c4f5c53)
- reconcile README with the migration + CI/CD (stripe_billing_meter, break-glass apply) (cb61032)
- clean pre-existing em-dashes in the README (repo no-dash law) (a2e5682)
- fix em-dash in the stamp code block (docs-token-guard) (6796bd8)
- pitch deck running plan + billing direction (Stripe, per-bundle) (09bb582)
- branded, marketable READMEs for every sub-repo (9c2a477)
- stop mentioning DNSSEC (no longer part of the design) (179a278)
- correct the license line (services are Apache now, only core is FSL) (f9681c9)
- record remediation results (8f69d08)
- marketable README template + brand mark + public-repo catalog (#148) (b585e9a)
- re-audit report - pass-5 gaps closed to A across all four subsystems (#145) (31b44d6)
- concrete specs for the three remaining follow-ups (#137) (af09b44)
- decouple hop-core's vocabulary from the sim (comments only) (#119) (d153989)
- pass-18 report - adversarial re-audit found 1 CRITICAL + 3 HIGH in this session's code (#101) (d130100)
- mark all pass-17 findings closed + recover the grades whose dip was fixed (#94) (ced8937)
- pass-17 report + close the docs/CI-guard findings it found (F-1/F-4/F-6/F-8) (#86) (a57b87f)
- §39 hardening report - same-id forgery class closed (r12-r15), hop-core A (#57) (14dba85)
- relay connection policy — adaptive check-in vs persistent WebSocket (ac14cae)
- recovery report - GPA slide reversed, 5 security vulns closed (#48) (61248ca)
- pass-4 report with a coverage grade folded in (#38) (a2d3576)
- gap analysis report + image-rich HTML; rescue FA mockup (e535d9e)
- both app cutovers done (compile-verified); crate rename deferred per UniFFI-stays design (d48af94)
- handoff current — both full stacks (SDK->bearers->driver) done; remaining = app UI cutovers (454c0f9)
- handoff current — tree migration + iOS xcframework done; remaining = the 2 app cutovers (d44ac8c)
- update handoff — driver done, both renames done, deferred Qs resolved (7a01f87)
- libhop architecture + bearer-isolation status (the morning handoff) (7fbfcd0)
- §32 — mark hps access modes, app-secret isolation, revocation as implemented (6860805)
- §28 cross-region — per-region local spools + online-only relay epidemic (a8ac7c3)
- §34 residency — detect for locality, declare for residency (a0968f7)
- §34 device inboxes — recipient-keyed mailboxes with locality migration (61e1984)
- §33 data protection & GDPR — durable store contents, residency, levers (47a86df)

### Features
- tag mirror releases so published packages can actually ship (0cd1018)
- finish inbound (import), drop export_pr (41c095e)
- auto-export changed components on merge to main (1c8c053)
- follow the system theme, no manual toggle (2964001)
- add the export_pr direction (monorepo PR -> mirror PR) (a472bbc)
- organization management (create + switch, persisted) (1392ae1)
- point the funnel at the live console (6f96a8f)
- §35 storage dimension, per-tenant held-byte occupancy (d58a544)
- store subscription/plan state from a Stripe webhook (59b16e0)
- team invites via Resend (create + accept) (6b P1c-invite) (6678a59)
- live subscription-state read for the console (6b P1d) (91beadb)
- team invite form + /invite accept page (P1c-invite frontend) (438556f)
- the dashboard — usage, keys, team, billing, observability (6b P3) (4f9e27d)
- Next.js signup + magic-link auth frontend (phase 6a) (509a334)
- near-realtime console usage read (6b P1b) (0bc06b9)
- console team management (6b P1c) (e4a6678)
- console read APIs — overview + invoices + card (6b P1a) (0fa2476)
- stream OTel to registry OTLP endpoints (P2, OTel day 1) (19916c2)
- relays + collectors read tenant keys from the registry (5b-2, reader) (f80f5b3)
- project the tenant registry to Firestore (phase 5b, writer) (e73d8d9)
- wire the billing endpoints into the console dispatch (4d) (2c7f561)
- carriage-key + OTLP registration endpoints (console phase 5a) (3a3d396)
- console billing endpoints (checkout + portal, phase 4c) (27ead52)
- Stripe write layer, self-serve checkout + portal (phase 4b) (119ab84)
- org/workspace bootstrap + tenant registry (console phase 4a) (46acd6d)
- auto-generate monorepo + per-library changelogs (git-cliff) (8c64c37)
- serve /auth/* + GitHub OAuth (console phase 3b) (67d591e)
- passwordless auth API + email seam (console phase 3a) (736e9da)
- Postgres Store binding + schema (console phase 2) (3363640)
- console foundation, passwordless auth + storage layer (81f8c68)
- add portable adversarial audit skill (b82e0e9)
- OTLP export, forward each tenant's telemetry to their OTel endpoint (51e25c2)
- rebuild the pricing page on the two-meter model with a live calculator (1cbac86)
- record usage history in our BigQuery, not just Stripe (4908c1a)
- the ledger source, closing the reconcile chain end to end (ba6b6b8)
- hop-accountd, the native invoice backend (stripe-account-key consumer) (62dbaef)
- Stripe meter-event emitter, with the money path unit-tested (41c9360)
- durable telemetry ledger, so metered usage actually bills (1e46b05)
- telemetry metering, tenant-attributed observability (§40 -> §37) (b823dcc)
- hop-telemetryd, the OTel-over-Hop collector (DESIGN.md §40) (3b30ae9)
- OTel-over-Hop telemetry transport (DESIGN.md §40) (8da170c)
- reach-based pricing (bill for offline delivery, not device count) (9635702)
- storage-dimension reconcile (windowed occupancy + own watermark) (e5aaa05)
- storage-dimension math in the reconciler (occupancy integral + GB-month) (6ca7f82)
- §37 reconciler crate (hop-billingd) - pure, idempotent, watermarked (3816c74)
- custody beacon (mode-1 HaveSet exchange) to cut duplicate-ingress COGS (708b565)
- bill the offline/spooled delivery path (attribute at durable re-ingest) (9558240)
- delivery/pull-justified metering + vaccine exemption (d35cc61)
- rotating key-hint carriage stamps (no tenant id on the wire) (a5e592d)
- §35 carriage stamps - keyed relays, per-bundle metering (wire v8) (4aae50f)
- hop-embedded, the PlatformIO library (ESP32/Arduino/MCU) (4dcd152)
- expose the endpoint CP quorum setter in all six SDKs (#161) (1bc8eef)
- phase 3 hold-until-coordinated quorum (CP; never double-process) (#159) (ab0f376)
- phase 2 rendezvous ownership (exactly-once when the owner is up) (#155) (6547ca8)
- cluster bindings across all six SDKs (+ passphrase ABI entry) (#154) (afb1632)
- self-clustering endpoints (phase 1 dedup) as a hop-endpoint-core layer over the mesh (#153) (487e4d2)
- Copybara mirror for sdk/node (the react-native/relay pattern, not fbshipit) (#147) (5f1c5cd)
- smoother wordmark draw-on, 32 subdivided ribbon pieces (#135) (f5a6eae)
- Crystal endpoint SDK (lib bindings, zero shards) + block/channel surface (#132) (a79eb23)
- example parity + in-process dev certs across go/python/node/elixir (#133) (d58c460)
- Ruby endpoint SDK (Fiddle, zero gems) + use-after-free-safe teardown (#131) (dbc1997)
- reachable-by-name over WSS + discovery (built-in :ssl, no WS deps) (#130) (d04187f)
- reachable-by-name over WSS + /.well-known/hop (pure stdlib, zero deps) (#129) (33a7552)
- reachable-by-name over WSS + /.well-known/hop discovery (#128) (c826292)
- reachable-by-name over WSS + /.well-known/hop discovery (consumes the reach record) (#127) (8d01c85)
- self-certifying reachability records (core + ABI) for DNS-free endpoint discovery (#126) (7c31123)
- wordmark draw-on animation (parts SVG + Lottie) (#118) (9b6d1b9)
- Go endpoint SDK via cgo (net/http-shaped, proven) (#124) (21e52ee)
- Python endpoint SDK via ctypes (Flask/FastAPI-shaped, proven) (#123) (2ef7c1d)
- Elixir endpoint SDK via a Rustler NIF (Phoenix/Plug-shaped, proven) (#122) (627c4b3)
- embeddable Hop endpoint SDK prototype (receive Hop messages in a Node app) (#120) (87c1592)
- festival area-swarm movement, channel broadcasts for mine/expedition/convoy, device-to-platform semantics (065015f)
- channels + mine scenario + curated homepage; fix two real protocol bugs the honest harness exposed (dc20947)
- real hop counts + LoRa net modeled + clockin & multi-hop everywhere + bubble timers (48b8e98)
- world scenarios (Istanbul/Manila/San Juan/Port-au-Prince/polder), multi-turn replies, event congestion, minichat, layout pass (7b8ee5b)
- scenarios play WHERE they happen (real geographies + topo maps), homepage embeds the live sim, LoRa Hop Bridge preview (8be7edc)
- sub-tick fidelity fix + scenario-check harness — all 11 scenarios verified to complete (7de5f9f)
- scenario engine — real-world scenarios played by the real mesh (06564ec)
- disaster components — grid power, ISP, cell; Starlink + battery-backed AP resilience (682c60c)
- sender-truth sublines (peers + sent-time, no omniscient 'received'); focus shows ONLY that conversation's lines (9c3ab12)
- per-transport SOAK (dwell-to-connect), reliable FA icons, HUD flex dock, emoji comb (077d1d2)
- ISP disaster toggle, anchored 'at a place' people, device Wi-Fi/known-networks, FA icon set, calmer narrator (461109f)
- convert UI emojis to Font Awesome; realistic Wi-Fi radii; fix qrow icon squish (60e0bcd)
- Font Awesome map icons + move APs off cell towers (a5c4c85)
- Wi-Fi access points — public/private, power + connectivity independent (622bfa5)
- Hop wordmark in the header + reliable one-tap conversation open (5669b41)
- beacon ripple viz + average delivery/ack cards (5599e1b)
- visualize the §39 gradient routing tree + fix store indicator (692ccca)
- §39 delivery vaccine — relays purge on ack, bundle-id only, no src/dst (16995ef)
- real hop-debug send status (Sent·N peers) + TTL cleanup + fix convo focus (42c5481)
- back each node with a real host Store (SQLite/OPFS) instead of wasm memory (9504585)
- decay a hop's line when its device purges the copy (not a hard cut) (c217b0e)
- flood driven by REAL held-copy state (not a timer) (13070a2)
- two-window conversation view (watch both phones) (bae8f42)
- run the real-node mesh in a Web Worker (off the main thread) (29b4248)
- drive the map from the REAL hop-core mesh (014a510)
- mesh.js — real hop-core swarm over a JS bearer shim (e6200a1)
- observe bundle-per-link transfers for the swarm viz (325a0ae)
- hop-wasm — run the real hop-core Node in the browser (8ff94bc)
- real messages as bundles + impersonate/compose (b4a1b61)
- cleaned wordmark — refresh site logo + OG card (8a0dd65)
- live map-based mesh simulator (8bfd36d)
- cleaned wordmark — refresh site logo + OG card (9496349)
- icon set — the Hop bug (cursive h → route arrow → node) (702c108)
- brand identity refresh — cursive mark, Poppins, icons + OG (964b6a0)
- light code theme — flip code blocks in light mode (90385c2)
- restore light/dark theming — business defaults light, devs choose (b744cda)
- docs site + drivers + unified interface reference + early-access form (8a6537e)
- protocol section + per-layer diagrams; drop LoRa; fix OG (63ca31d)
- redesign — "Signal" dark theme + interactive scenario player (2528cef)
- linkable layer stack — use-cases + docs pages; "encrypted" copy (973690a)
- carry L2CAP PSM in the BLE advert; gentle bursty Android central (650b353)
- tap-to-retry a 'Not sent' message (494ab17)
- sticky 'direct' — keep a once-direct peer in Nearby while reachable (8d4e892)
- persistence story (deferred sends, invites, channel threads, re-advertise) + direct-by-link categorization + multi-peer BLE (50befb1)
- direct/mesh peer sections + per-peer link-type indicators (iOS parity) (7fd2454)
- Channels stack nav parity (access modes, invites, browse, revocation) (a94f592)
- Channels stack nav + access modes, invites, browse, revocation (68ec5ed)
- expose hps access modes, invites, discovery, reach, revocation + app secret (c89e71b)
- hps access modes, invites, discovery, reach, revocation (steps 2-3) (8c3f1cc)
- app-secret key material + directory app-scoping (hps isolation step 1) (7911614)
- persist chat history, unread app badge, not-sent state, relay pinning (a2b3cdb)
- multipart messages — text + multiple images as one payload (both platforms) (73229db)
- hdp/hops/HNS architecture — hops:// wiring + hop-endpoint + DESIGN (§30/§31) (9b0a7c5)
- robust delay-tolerant ACKs + DTN-scale lifetimes & retx backoff (§7) (7833d54)
- forward-before-evict custody policy + bigger cloud relay window (§6) (96f671b)
- transparent carrier-streaming — any message/service auto-chunks (§20) (5bdb8c4)
- drop open-web HTTP fetch; document hops:// origin-gateway model (d4dbf33)
- add a contact to the address book by base58 address (cc759de)
- handoff logs show msg id+dest; app surfaces messages from unknown senders (e29f06b)
- clear-relay-queue button + Node.clear_queue / FFI (0f306bf)
- live network-log stream over HTTP (§ops) (c202ef0)
- identify-resolved traces, relays in list, per-link symbols (§29) (324133b)
- service calls (hop.identify) + per-region subdomains phase 2 (§29) (681fd87)
- per-region subdomains (phase 1) — wildcard cert, host routing, dual-stack (f28065b)
- cross-partition handoff — the offline-destination mailbox (§28) (201f48a)
- device check-in — auto-connect + auto-reconnect to relay.hopme.sh (ec48171)
- per-region identity + wire --region/--advertise (§28) (1530b32)
- surface traces+routes in app; stamp carrier app id per hop (7b3772e)
- §28 liveness registry + pull-on-wake dialing (36dfd09)
- §27 provenance traces + learned routes + utility-prioritized epidemic (ff6318f)

### Other
- rebuild sim/pkg for the 0.0.2 bump (20d8cae)
- bump our crates in every standalone/vendored Cargo.lock (aad3ff7)
- bump every published component to 0.0.2 (7b1ffab)
- say plainly that SVC-001's literal wording is not met, not that it is (133b36f)
- make the writer-scoped ledger readable end to end, and stop overclaiming (5ee2555)
- writer-scope the usage ledger rows so concurrent flushes compose (SVC-005) (b66cac9)
- gate the live log fan-out, not just its backlog (SVC-001) (935cba3)
- give local-ci-mirror per-run scratch, so parallel runs stop corrupting each other (ef837fa)
- scope two vaccine-budget claims to what they actually bound (bb5302b)
- give the deferred vaccine drain its own pass budget so a flood cannot starve it (8d15430)
- stop the vaccine deferral queue dropping genuine vaccines, bound the gradient quota hot path (975cdc7)
- bind the gradient quota to the peer, stop vaccines occupying custody, fix three §39 defects (ca7c9dd)
- scan fuzz, mockups, and business, which were in neither pass (a3cd04f)
- repoint link_framing_reassembly at the framing that actually runs (97fa9fb)
- extract Multipeer so EVERY bearer registers with the manager (f3949f7)
- rate-limit inbound receiver-beacons per link (c1c0c49)
- stop the receiver-beacon publishing address-to-mailbox in cleartext (5948b7c)
- rebuild pkg after set_route_to_me changed the wasm interface (183dae3)
- rebuild pkg for the wire v12 bump (7019990)
- the local CI mirror green-lit work CI would reject (ee4ecdf)
- a stopped Android central revived itself from an in-flight scan callback (ea43289)
- per-transport switches, and show when the SENDER sent a message (7a923c0)
- let a host enable and disable individual bearers at runtime (c17b01a)
- move the BLE socket write off the node's core thread (R-02) (41fd3fb)
- heal a doomed handshake on the private path, where no reset can be sent (5133f4d)
- rebuild pkg after the wasm bridge lost set_data (16dfe00)
- cut the frame cap to the protocol's, and pin it (2a16945)
- delete unwired trait surface and correct 12 stale claims (4e4272f)
- clean the em-dashes retiring store.rs from the wire manifest exposed (59fa5f2)
- Apple was resetting the dial backoff at the wrong moment (f94599f)
- wire the relay pool end to end, and stop the wire guard false-firing (35946e0)
- delete the dead copy-budget API and stop the simulator lying (9ab3138)
- fix Apple's dial backoff and pin the schedule across platforms (54f6f02)
- relay pool with health scoring and failover (92f2eea)
- rebuild pkg for wire v11 and repoint the corpus checks (af62d5f)
- one-time prekeys for async first contact (wire v11) (d6ebce3)
- adopt the pre-existing Resend domain instead of creating it (bae4a2a)
- fold Resend domain into the billing root (drop the separate apparatus) (47df101)
- de-flake the LAN pending-cap test (hold the no-HELLO reaper) (8497488)
- register the Resend sending domain in Terraform (infra/resend root) (753838a)
- rustfmt the oauth redirect_uri assertion (6ee3865)
- rework the console deploy onto the new GitHub-is-change-management pipeline (b53b9d0)
- strip em-dashes from the OpenTofu comments (f5b7cba)
- eliminate every manual secret-seeding step (e26b791)
- reachable accountd ingress + wire STRIPE_WEBHOOK_SECRET (65e301c)
- deploy hop-accountd + hop-console to hop-mesh (9ebe655)
- narrow the manifest off access.rs to the carriage-stamp layout (e3d032d)
- apply bootstrap from CI, never a local terminal (562ceca)
- close the retirement blind spot with a live corpus regeneration (0dffad9)
- narrow the manifest to code that decides wire bytes (b58fed8)
- Resend sending auth for account.hopme.sh (b557cae)
- accountd firestore image + the deploy plan (P4) (d51be5d)
- reject internal/metadata OTLP endpoints (5a review) (8518833)
- block agent environment disclosure (882b3d3)
- pre-stage the stripe-account-key secret container (b215683)
- CI/CD apply via GitHub Actions + Workload Identity Federation (b85517e)
- apply.sh helper (one-command catalog create + prints the ids the metering system needs) (99adf43)
- migrate to the OFFICIAL stripe/stripe provider (lukasaron archived) (85ad206)
- stripe-api-key secret container + BigQuery dataset + billingd SA (1cdf8b3)
- box the envelope stamp (clippy large-variant), fmt, §35 addendum (6b601b6)
- label hop-embedded FSL-1.1-ALv2 + restore PlatformIO in bootstrap (b00cf47)
- treat copybara no-op (exit 4) as success (636fd3b)
- wait for the crates.io index after each crate publish (bc96f4f)
- RubyGems trusted-publishing release workflow (rake release + OIDC role) (5a2d6ee)
- publish the Rust crates under the hop-mesh-* namespace (3bb9d0c)
- local first-publish + OIDC trusted publishing on npm/PyPI/RubyGems (beefc71)
- CLA gate on contributions (preserve commercial relicensing of core) (5a9aa7d)
- bootstrap-packages.sh (accounts + registry tokens + first-release) (a7b7110)
- scope the packages under @hop-mesh (d53b7aa)
- SECURITY.md per component + enable-security in the bootstrap script (a1492e9)
- copyright holder is Hop Mesh, LLC (7d8c514)
- fill the Apache-2.0 copyright placeholder (2026 Jason Waldrip) (2fb7d1c)
- Apache-2.0 for everything except core/ (only the protocol stays FSL) (0fe9439)
- dump the copybara wrapper script + locate the real jar (48b8ba1)
- inspect olivr/copybara image entrypoint + CLI (2ea501e)
- CHANGE_REQUEST sync-back + document merge/conversation + confidentiality (9e1dec2)
- add the multi-component config, dispatch workflow, and README (efaf977)
- wire mirroring for all 20 components (export + import) (fad3bf6)
- bootstrap script to create + publish the SDK mirror repos (#163) (e33c204)
- make the TLS-served reach record the only name path (drop DNSSEC-over-DoH) (#139) (8998288)
- one consistent endpoint surface across node/python/go/elixir (#125) (c46cd8d)
- gate HopDriver coverage from llvm-cov JSON, not a positional awk column (F-CI-7) (#102) (5e5f539)
- close 5 guard gaps pass-18 found (anchor-job bypass + 4 dash/matrix holes) (#98) (67376df)
- verify_publish uses verify_strict, matching the crate-wide ed25519 convention (#95) (9f40004)
- migrate to Astro 7 (land Dependabot #23) (#88) (339caaa)
- lift HopBearer+Hns.swift 59% -> 99% + add a per-file coverage floor (#83) (4bfb245)
- prevent_destroy on the anycast IPs + relay-identity secret (2nd-pass finding) (#80) (6ab7c9b)
- decompose the 1489-line HopBearer god-object into per-concern collaborators (C+ → A) (#76) (bb4c68d)
- decompose the 1895-line HopBearer god-object into per-concern collaborators (B- → A) (#75) (4d86f9d)
- route dedup through the pure keep-rule cores; fix inverted Android dedup-ordering docs (#72) (8a083a1)
- close the two capping defects in Endpoint and the Kotlin SDK (#74) (aa338be)
- root-cause the recurring 0-byte LICENSE bug; version floors; guard (all → A) (#73) (eee7beb)
- strip em-dashes from this session's Apple coverage test files (#67) (f11147f)
- canonicalize the delivery-vaccine shape at verify (r15-01) — close the is_ack twin on the vaccine id (#56) (3a1c666)
- bind the entire private inner into the wire id (r14-01) — close the flags.request_ack twin residual (#55) (23228cf)
- rebuild wasm pkg for wire v5 (r13 header-bound private id) (#54) (e338343)
- bind the §39 recognition header into the private wire id (r13-01) — close the 7th chimera vector at its root (#52) (0b6e366)
- cargo fmt the r12 chimera test (rustfmt line-wrap) (#51) (6c7fb6d)
- close the §39 recognition-header chimera (r12-01) — the 6th vector (#50) (e97faed)
- authorize the response-cleanup purges (r11-01) - close the last purge vector (#47) (69cdbed)
- enforce the §39 private-bundle invariant at the gate (r10-01 root fix) (#46) (9450375)
- close the traced-ACK forgery on the DEFAULT private send path (r9-01) (#45) (cc16340)
- close the private-chimera bypass of the traced-ACK authorization (r8-01) (#44) (bf3168b)
- restore LICENSE.md (FSL-1.1-ALv2) - the reaudit-close restore did not land (#35) (aa4dadb)
- fix Android dial-backoff wedge caught by the hardware gauntlet (#16) (bc47eab)
- cargo fmt + clippy across the touched crates (c6216a1)
- relay is the cloud, not a place — drop the relay marker + backbone lines (678dc70)
- start cell-down, tag msgs with bundle at send, focus selected convo's path, list holds until ACK (9a6ed8a)
- measure sqlite-wasm heap too + throttle map setData to 33fps (dd3fa1e)
- readout now shows worker wasm memory + SQLite row count (locate the 3.9GB) (6330087)
- live heap readout + bound viz state; throttle holder scan (OOM hunt) (8458629)
- livelier auto-send (every 3s, up to 6 in flight) now that storage is on disk (da460f0)
- stretch compressed bundle TTL to ~1 sim-hour (relays hold copies longer) (2079049)
- fix the relay-teardown destroy cycle by count-gating the whole https chain (02b42da)
- gate relay observability on relays_enabled + grant build SA logging.admin (f7c048c)
- take the relay fleet OFFLINE (relays_enabled=false) for the P2P test phase (a2f0097)
- reflect the shipped app cutover + SQLCipher deploy wiring (7e737ba)
- mark the 4 partials fully closed + deferrals done (33e1f95)
- SQLCipher encryption at rest, keyed through the whole SDK stack (777cdb9)
- local crash logging (both apps) + testkit comment fixes (79f5513)
- DNSSEC Ed25519 + RSA modulus floor; document the 429 ceiling (e210ba7)
- session GC, sqlite schema guard, remove dead k-bit fields (103084e)
- put every fix in a green box; fix blank-PDF print bug (89500fb)
- annotate every finding with its resolution (d1ea10d)
- Font Awesome (Light) icons everywhere, replacing SF Symbols (f3ebae5)
- fix FaIcon clipping + replace ALL remaining emoji with FA Light (2398b2a)
- Font Awesome (Light) connection icons, replacing emoji (1295650)
- remove the Wi-Fi Direct bearer (per-device approval dialog) (c059d69)
- optimistic send — instant bubble, decouple from the core thread (e8714ff)
- remove dead Spacelift bootstrap + tighten cloudbuild SA off roles/owner (33c2bee)
- supply fleet vars as TF_VAR_* env on the apply step (6310299)
- migrate GitOps off Spacelift → GCS state + Cloud Build (OpenTofu) (e029ceb)
- refresh GATT cache on dial timeout to break the discovery wedge (94e0315)
- relay-queue label → "broadcast", fix iOS endpoints misclassification + Android jumbled service log (978d085)
- remove Destination::InternetEgress (mesh-visible internet-bound leak) (5dd64d3)
- app identifiers + code packages net.waldrip.* / co.hopmesh.* -> sh.hopme.* (9050fbd)
- JNA compileOnly to avoid jar+aar duplicate-class clash (595cf3a)
- fix the stale F0900BEA beacon UUID -> 7ED7BEAC in the Android driver (9f9bad6)
- split into HopContract (no libhop) + Hop (libhop node) — unblocks the app cutover (7f0eeb3)
- thin HopDriver composing the SDK + all four bearers (mirror of Apple) (171b04a)
- re-home all four bearers as independent modules on the Kotlin SDK (fd72ebe)
- ship libhop as an xcframework binary target (device-buildable stack) (101bc9e)
- thin HopDriver composing the SDK + all four bearers (889fe62)
- rename the cdylib/staticlib hop_ffi -> hop (the C ABI is `libhop`) (b7c9823)
- rename sdk/wrappers/swift -> sdk/wrappers/Hop (clean SwiftPM package id) (ee6245c)
- contract-purity guardrail — no transport symbols in the contract/SDK (587b6e8)
- bearer kit at parity with Swift (Bearer/LinkSink/BearerManager/HopRuntime) (3db23a1)
- a full Hop client in pure C over hop.h (6505ebf)
- idiomatic Kotlin/JVM wrapper over hop.h (JNA) + smoke (0fe8e27)
- add the hops:// request/response surface to the C ABI (85a95ff)
- RuntimeSmoke — prove HopRuntime + a Bearer drive the node end to end (c794c63)
- re-home all four bearers as independent packages on the Hop SDK (05124fe)
- ship the bearer kit with Hop (Bearer/LinkSink, BearerManager, HopRuntime) (bfaf29e)
- idiomatic Swift wrapper over hop.h (Hop) + macOS smoke (d1e91a6)
- expand the C ABI into a complete client SDK (e0d2a6d)
- a cbindgen C ABI for hop-core — the universal client + bearer contract (25bcf5c)
- emit a timestamp `expireAt` so the TTL policy actually sweeps (0133776)
- add `relaymac` — a headless RELAY-ONLY macOS test client (d39d170)
- request an OAuth scope for the metadata-server token (fix relay 401) (64ff7e6)
- add Role.relayOnly — relay and nothing else (no BLE/LAN/Wi-Fi) (e74c709)
- guard UNUserNotificationCenter for iOS only (macOS CLI crash) (e8c5016)
- make untraceable messaging the DEFAULT + private delivery-ACK (82371c5)
- tag shared-bearer peers by real transport in the prod UI (78070c7)
- cloud relay as its own lib (both platforms) + re-enable the relay (6bccaa2)
- Wi-Fi Direct as its own lib + register in clean-room & prod (014ac20)
- add .gitignore, untrack 1074 committed build artifacts (a79216a)
- fold the shared HopBearers libs into the prod path (App→hop core→bearers) (3ab617c)
- BearerManager(baseLinkId:) to offset the global link-id space (e93353e)
- LAN bearer as its own lib (Bonjour + TCP) + thread-safe manager (94e7c22)
- split into core + one lib per bearer (no master library) (b6cf03e)
- add .gitignore, untrack committed build/.gradle artifacts (575f7f9)
- README — the one-interface contract + how to write/register a bearer (ffea796)
- deterministic registry tests (radio-free proof of multiplexing) (c30c88b)
- transport-agnostic Bearer registry (BearerManager) + BLE extraction (b2caf75)
- shared bearer-library keystone — LinkSink/Bearer contract (545fb43)
- NSLog alongside print so the iOS device log is capturable for L2CAP-teardown diagnosis (2629e04)
- fix BLE redial storm (prefix-less peers dialed every advert) + bug-hunt findings (a48ae7a)
- clean-room validation results + 4-device harness config (8fd4e16)
- fix multi-peer flood + securing/delivery; P2P test mode + harness (656b62b)
- stop the re-gossip directory re-flood (multi-peer resource exhaustion fix) (6e98be4)
- prove duplicate-link handling is sound; add BLE handshake-flow diagnostics (2d20843)
- pure-L2CAP with advert-carried PSM + platform-biased roles (Ditto pattern) (8e76cb1)
- one-pipe-per-pair gate, immediate fallback, skip L2CAP to Android (2ee58c9)
- always discover GATT services so the GATT-data fallback can find DATA (e6eacee)
- flush deferred ("Securing") content when a session forms, not just on tick (b3c23ab)
- auto-heal a wedged BLE stack when Bluetooth cycles (fa82208)
- re-gossip adverts over stable links so prekeys propagate without a reconnect (f81839d)
- keyboard-open layout + offline label (01f4aa0)
- fix frozen keyboard — drive SendingIndicator pulse from the 1s tick (76b79bd)
- broadcast iBeacon to wake backgrounded/killed iPhones (cross-platform wake) (0405337)
- GATT-first by default, Android-only filter, 0xFFFF-mfg PSM guard, backoff reconnect (ff27225)
- run BLE pure-peripheral — central scan was starving discovery (91698d4)
- require ratcheting for device-to-device content (fix lockless sends) (ae44f70)
- diagnostics parity — service log, relay queue, hop traces, identify (5aae021)
- unified 4-tab information architecture across iOS + Android (6c73802)
- hops:// WebView browser over the mesh — full iOS parity (d62de94)
- hps:// pub/sub, hops:// fetch, HNS — feature parity with iOS (2bd262d)
- image send + display (feature parity, step 1) (1cef2b9)
- auto-connect + auto-reconnect the cloud relay (delivery parity) (e76260b)
- // pub/sub phase 3: FFI surface + iOS channels/services UI (3e2dcb6)
- regenerated Info.plist — BGProcessing task id permitted (c593a84)
- reconnect the relay on background wake + BGProcessing backlog drain (2a7e16c)
- online-only relay-to-relay epidemic (gated, off by default) — §28 phase 1 (af0bdc9)
- keep relay custody of request_ack bundles until the delivery ACK (7f7042e)
- persist carrier reassembly so large transfers survive background wakes (a237013)
- track chunked-message delivery status under the original id (5a83412)
- add HopWebView.swift to project.yml sources (355f43c)
- SessionReset — recipient heals a desynced ratchet (d576c7b)
- persist forward-secret ratchet sessions across restarts (466ba39)
- link-layer fragmentation for oversized records — fix silent drop (f915d13)
- // pub/sub phase 2: node wire flow over Broadcast (20781a3)
- hps:// pub/sub crypto primitives — phase 1 (DESIGN §32) (46b234e)
- DNSSEC ECDSA P-256 (alg 13) verification (8e53db5)
- carry the DNSSEC proof in HnsAnswer — trustless multi-hop (phase 3) (eca489e)
- enforce DNSSEC end-to-end — host fetches chain, core validates (phase 2c + 4) (fb49ca7)
- validate_and_extract — one-shot trustless resolution (phase 2c) (4835819)
- DNSSEC chain assembler from DoH responses — phase 2b (832bb2a)
- DoH JSON parsing — phase 2a (0b22411)
- DNSSEC chain walk to the root anchor — phase 1b (eb6e403)
- DNSSEC validation primitives (RSA/SHA-256) — phase 1 (53ec384)
- decentralized multi-hop DNSSEC HNS + §32 hps:// pub/sub (57e523e)
- delay-tolerant resolution + utf-8 error bodies (636aafb)
- fix dial_relay deadlock (non-blocking socket) (6596d5f)
- a no-internet node resolves by broadcasting to connected peers (5afab58)
- prune expired records so they leave the device (5140d60)
- routable leaf on the mesh + app labels endpoint by its domain (d47ccab)
- list hops:// endpoints separately, not as relays (749d243)
- render scheme-honestly (hops:// vs https://) (126841e)
- // WebView + content-type plumbing (1b4950b)
- dial the hops:// endpoint directly so requests actually deliver (d5f7e5f)
- also serve standard HTTPS (reverse-proxy to origin) (454bb41)
- rename build SA to hop-cloudbuild (avoid soft-delete collision with probe) (40598c1)
- build SA for the Cloud Build trigger (fresh project has no legacy CB SA) (11831a5)
- only infra relays stamp a trace app id; devices stay fabric (de39f8a)
- 2nd-gen Cloud Build GitHub connection for GitOps trigger (fa31ffc)
- GitOps build trigger (GitHub App, gated) + sha-tracking deploy + region allowlist (d850217)
- deploy Cloud Run relay to all available regions (150364d)
- drop timeout_sec on serverless-NEG backend (unsupported) (e2a82c2)
- enable global LB anycast + relay.hopme.sh A record (40a5e1b)
- create Cloud DNS public zone for hopme.sh (b26aafa)
- remove one-time import blocks (resources now in Spacelift state) (bfc2e4b)
- fix cloud run IAM-member import ID (space-separated) (5d34c7f)
- fix IAM-member import IDs (space-separated form) (8eb8492)
- temporary import blocks to adopt live resources into Spacelift state (62a725a)
- stop tracking the Spacelift credential config (25bb9e2)
- Spacelift↔GCP OIDC bootstrap (ced94ee)

### Performance
- render the deck overlay at ~60fps (was 33fps) for smooth marker motion (798b9a5)
- cache the device connectivity badge per tick (was recomputing turf per frame) (ce1fa23)
- render people + lines with deck.gl overlay, not MapLibre setData (5d24597)
- people as DOM markers, not per-frame symbol layers (the 4GB leak) (f745550)
- BLE scan BALANCED not LOW_LATENCY — free radio time for the peripheral role (f18f55c)

### Refactor
- GHA runtime-deploy via WIF; trim guards + delete deploy-provenance apparatus (956b711)
- delete the GCP deploy trust apparatus from bootstrap + runtime TF (8c34b2c)
- fold billing authz onto the shared authorize_tenant (9f529dd)
- enforce purpose/platform/package (collapse sdk/wrappers, apps/web -> apps/web/site) (#116) (afd52df)
- remove the orphaned apple/HopBearers package (2ae1d75)
- reorient defense page to neutral disconnected-ops capability page (9add516)

### Reverts
- drop the short send TTL — it murdered slow DTN deliveries before they arrived (b238e3e)

### Testing
- replace a tautological custody assertion and correct two more overclaiming comments (890cba1)
- de-flake the WSS pending-cap recovery test (8dd7bbb)
- export_pr validation probe (sdk/node) (dddb024)
- cover a second retirement and wrapped retirement records (e53b687)
- enforce portable skill contracts (dbb7629)
- tolerate inbox journal compaction (6a33d64)
- verify durable custody acknowledgements (b86e9da)
- kill the unseeded stadium delivery-gate flake (seed-retry fallback) (#140) (5dd650f)
- close store.rs and hps.rs coverage gaps to 100 percent (#81) (0f9ad2d)
- extract DemoFormat + 21 tests, testable surface 0% → 100% + CI gate (#71) (c041a14)
- extract HopDemo pure logic into HopDemoKit + 100% headless coverage gate (#70) (686d3af)
- seam refactor takes BleBearer 7% → 97% (CB-free cores), replace shadow tests (#69) (36f184b)
- LAN 95% / Relay 100% / BLE 99% + fix non-restartable LanBearer (#68) (0fab82d)
- raise host-testable coverage to A (99%) by splitting off the wasm-only surface (#66) (ae6bb31)
- Robolectric suite takes the driver from D (~16%) to A (94% line) (#65) (112ea0f)
- headless-node suite raises HopBearer.swift 12.8% -> 88.5% (F -> A) + CI coverage floor (#64) (75f4507)
- real loopback integration tests for LAN + Relay bearers to >=80% coverage, CI gating, compile-bug root cause (#63) (c53d864)
- raise hop-relayd line coverage 41.2% -> 81.1% (#62) (40dd07b)
- cover the JNA surface against real libhop to 99.5% (F → A) + CI coverage gate (#60) (883e4ff)
- raise line coverage 39.4% -> 80.9% (#61) (1510b15)
- cover the FFI/UniFFI HopNode surface + free helpers (crate 56.5% -> 96%) (#59) (af90faa)
- cover the REST layer to 96.7% (from 40.5%) (#58) (1222bf3)
- robustness matrix — appboot + seeded-jitter harness modes; every scenario verified under perturbation (80be802)
- refresh fleet address map (sh.hopme.demo identities) (81a7332)
- space Android deep-link sends to avoid LAUNCH_MULTIPLE drops (582fa6e)
- re-add bush + Decklan's iPad to the 6-device fleet (91b663a)
- add bush + ipad to the address map; vscode hopmac debug config (66d9edd)
- exchange prekeys in ffi + ws round-trip tests (require-ratchet) (e261e8b)
- out-of-order SessionMessages decrypt through the node accessor (2e9b251)
- verify fully hands-off autodeploy (push only) (cf29998)
- trivial change to exercise autodeploy + before_apply hook (7f14615)

