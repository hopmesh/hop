# Changelog

Notable changes, generated from [conventional commits](https://www.conventionalcommits.org) by
git-cliff. Do not edit by hand.
## Unreleased

### Bug Fixes
- drop DNSSEC mentions + reframe the hops:// example as a hosted service call (#156) (83fe08b)
- stop the San Francisco flash on load + light-mode legibility (#160) (5059de7)
- make the light theme legible (HUD accent text + map labels) (#152) (096f1e5)
- Quickstart nav button text was dark, not white (specificity bug) (#151) (7af9af8)
- unreadable white-on-mint primary buttons in light theme (#150) (c7bea0f)
- re-ingest mailbox pulls via LOCAL_LINK so a >cap backlog isn't dropped-after-delete (#143) (81a9c56)
- wordmark draw-on was 'just the dots' (opacity ramp had no easing handles) (#142) (149ba75)
- well-known reach record was born expired (issued_at=0) (#141) (c2e7235)
- pass-5 audit remediation - DNSSEC name-hijack (CRITICAL) + Node reply UAF (HIGH) (#138) (74fd6ca)
- use-after-free-safe teardown across go/python/node (+ elixir safety test) (#134) (f174422)
- close F-18d, HpsRekey fails safe under a mid-arm panic (#104) (1d3e810)
- root-cause + close the bearer-lan JaCoCo coverage wobble (F-3) (#103) (27c4ffa)
- check-pkg-fresh treats an untracked committed pkg file as DRIFT, not a fallback (F-web-1) (#100) (987b3f6)
- HopStatus.forwardHops is UByte (F-1, the F-9 sibling) + a real reinterpretation test (F-2) (#99) (4ac81ce)
- repair the branch-protection audit that PR #85 broke (invalid permissions scope) [CRITICAL] (#97) (6057628)
- close two bypasses in the F-7 rate cap (F-18a unbounded map, F-18b pre-auth reset) (#96) (3da0781)
- exercise publish_recv_beacon/send_traced/set_default_lifetime_ms/inbox_debug through real wasm (#92) (ce01d26)
- close F-7 - per-node-identity rate cap on the driver's Ev::Data (#93) (0fba8f6)
- panic-isolate the driver loop (F-2, HIGH) - close an unauthenticated remote-DoS gap (#87) (de037f4)
- make the branch-protection audit actually pass (token scope + context format) (#85) (e3c13ac)
- authenticate the require-ci deploy gate for the PRIVATE repo (3rd deploy blocker) (#84) (6e14ae6)
- unbreak the GitOps deploy - it was doubly broken and red for 40+ builds (#82) (7941205)
- make hexd byte-safe so a hostile DS-digest can't panic the resolver (2nd-pass audit finding) (#78) (20375b0)
- cache image decode + downscale picked photos (fixes iPad CPU crash + picker hang) (22f3a2f)
- HopLink keepalive + liveness watchdog (match iOS) so links don't drop every 15s (9287cdd)
- a live BLE/Wi-Fi link is 1 hop — drop sticky 'direct', categorize by hops (7d341e4)
- periodically re-arm BLE advertising to self-heal a silently-stopped advertiser (23f6e4a)
- scan with allowDuplicates so peers (esp. Android) get repeated connect chances (b1a7562)
- handle didModifyServices — re-read L2CAP PSM after an (Android) peer restarts (3c57151)
- direct = live BT/Wi-Fi link OR <=1-hop advert (no flicker to mesh) (c44cf2e)
- connectGatt on main thread; BLE link diagnostics (426f92f)
- BLE GATT lifecycle — close failed connects, allow retry, clear on link drop (868f757)
- BLE L2CAP PSM is 2 bytes (match iOS), enabling cross-platform direct links (087cb74)
- restore hosted/subscribed channels on restart (4cdc724)
- sort peers by stable address; android uses user-assigned device name (c41adf2)
- channel host now receives members' posts (5957fe6)
- set node clock before publishing prekey so sessions form (8f1486e)
- stop SIGABRT crash from openL2CAPChannel on bad peripheral state (2451135)
- a ServiceResponse purges its request (return-path delete, §29) (348fd1d)
- answer non-WebSocket HTTP requests with 200 (Cloud Run health) (62b2edb)
- transport counts reflect real links; Wi-Fi indicator tracks the radio (5799af9)
- remove peer dialing — handoff-only, so nodes never wake nodes (§28) (dbb5af2)
- recreate https proxy on the cert map (in-place swap 412s) (5fb746b)
- keep old managed cert so the proxy cert-map swap applies cleanly (9f13dea)
- gate Certificate Manager resources on API enablement (phase 1 retry) (50aa219)

### CI
- auto-merge Dependabot PRs on green (squash) (#157) (8cda2bf)
- sdk/node CI as a canonical composite action (shared monorepo <-> standalone repo) (#149) (2eb8226)
- gate the six server SDKs (node/python/go/ruby/crystal/elixir) (#136) (ddfa09e)
- before_apply hook to wait for the commit's image before deploying (0cc2278)
- build on every push + activate SHA-tracking deploy (b410d87)
- commit Cargo.lock + GitOps build trigger (1st-gen GitHub) (9eb3b72)

### Chore
- bump the node-sdk-dependencies group across 1 directory with 2 updates (#158) (36bc798)
- bump org.json:json in /apps/android/HopDemo (#107) (2de7004)
- bump org.robolectric:robolectric in /apps/android/HopDemo (#110) (d10dfaf)
- bump androidx.test:core in /apps/android/HopDemo (#109) (887b7ed)
- bump androidx.test:core in /bearers/android (#113) (ac1ef41)
- bump androidx.activity:activity-compose (#108) (9dcefae)
- drop the root license, license per-component (FSL-1.1-ALv2) (#146) (2e68a4b)
- drop the dead root site/ scaffold's gitignore entry (#117) (2c9df3c)
- finish the monorepo layout, kill platform stubs, unify the platform axis (O-1/O-3/O-4/O-5) (#115) (edaf8fc)
- organize the monorepo, all apps under apps/<platform>/<App> + a CLAUDE.md at every tree node (#105) (98c5613)
- bump net.java.dev.jna:jna in /sdk/wrappers/kotlin (#18) (cfcbd90)
- bump gradle-wrapper from 8.9 to 9.6.1 in /android/HopDemo (#28) (2511265)
- bump com.google.zxing:core in /android/HopDemo (#29) (2a7c7e9)
- bump jvm from 2.0.21 to 2.4.0 in /sdk/wrappers/kotlin (#26) (bb546b8)
- bump net.java.dev.jna:jna in /bearers/android (#32) (d3af49c)
- bump the github-actions group across 1 directory with 10 updates (#19) (6627545)
- untrack the Kotlin SDK's machine-local build artifacts (caught by the final re-audit) (#77) (378cc89)
- log GATT-server connection state (BLE cross-platform diagnostics) (3da725e)
- BLE diagnostics (advertise/scan/accept/PSM-read logging) (388df47)

### Dependencies
- land the grouped rust-dependencies bump (sha2, ed25519/x25519-dalek, chacha20poly1305, snow, rusqlite, p256, uniffi, tungstenite) (#89) (f09a43f)
- Kotlin 2.4/AGP 9.2.1/Compose BOM 2026.06/okhttp 5.4 toolchain migration (#90) (1f83a56)

### Documentation
- regenerate from conventional commits (3912d31)
- marketable README template + brand mark + public-repo catalog (#148) (95645ec)
- concrete specs for the three remaining follow-ups (#137) (ac5fed6)
- decouple hop-core's vocabulary from the sim (comments only) (#119) (48243e4)
- pass-18 report - adversarial re-audit found 1 CRITICAL + 3 HIGH in this session's code (#101) (e9c6285)
- mark all pass-17 findings closed + recover the grades whose dip was fixed (#94) (8e657e2)
- pass-17 report + close the docs/CI-guard findings it found (F-1/F-4/F-6/F-8) (#86) (b2c61a1)
- §39 hardening report - same-id forgery class closed (r12-r15), hop-core A (#57) (6800657)
- recovery report - GPA slide reversed, 5 security vulns closed (#48) (875d372)
- pass-4 report with a coverage grade folded in (#38) (8d16f11)
- both app cutovers done (compile-verified); crate rename deferred per UniFFI-stays design (ff19c17)
- handoff current — both full stacks (SDK->bearers->driver) done; remaining = app UI cutovers (9f967e5)
- handoff current — tree migration + iOS xcframework done; remaining = the 2 app cutovers (69460e5)
- update handoff — driver done, both renames done, deferred Qs resolved (f9cb156)
- libhop architecture + bearer-isolation status (the morning handoff) (ae02bbb)
- §32 — mark hps access modes, app-secret isolation, revocation as implemented (9be6330)
- §28 cross-region — per-region local spools + online-only relay epidemic (00f1617)
- §34 residency — detect for locality, declare for residency (9cbc320)
- §34 device inboxes — recipient-keyed mailboxes with locality migration (fcb8ab1)
- §33 data protection & GDPR — durable store contents, residency, levers (f5be455)

### Features
- expose the endpoint CP quorum setter in all six SDKs (#161) (132e97e)
- phase 3 hold-until-coordinated quorum (CP; never double-process) (#159) (65d8103)
- phase 2 rendezvous ownership (exactly-once when the owner is up) (#155) (890a543)
- cluster bindings across all six SDKs (+ passphrase ABI entry) (#154) (4dfae0f)
- self-clustering endpoints (phase 1 dedup) as a hop-endpoint-core layer over the mesh (#153) (9b56cd5)
- Copybara mirror for sdk/node (the react-native/relay pattern, not fbshipit) (#147) (5483e87)
- smoother wordmark draw-on, 32 subdivided ribbon pieces (#135) (c908c4b)
- Crystal endpoint SDK (lib bindings, zero shards) + block/channel surface (#132) (0d0bd17)
- example parity + in-process dev certs across go/python/node/elixir (#133) (4cd101f)
- Ruby endpoint SDK (Fiddle, zero gems) + use-after-free-safe teardown (#131) (11baa97)
- reachable-by-name over WSS + discovery (built-in :ssl, no WS deps) (#130) (100b2f8)
- reachable-by-name over WSS + /.well-known/hop (pure stdlib, zero deps) (#129) (b519c49)
- reachable-by-name over WSS + /.well-known/hop discovery (#128) (21a177c)
- reachable-by-name over WSS + /.well-known/hop discovery (consumes the reach record) (#127) (2365ef8)
- self-certifying reachability records (core + ABI) for DNS-free endpoint discovery (#126) (e0bae40)
- wordmark draw-on animation (parts SVG + Lottie) (#118) (e0c6188)
- Go endpoint SDK via cgo (net/http-shaped, proven) (#124) (f7d28c0)
- Python endpoint SDK via ctypes (Flask/FastAPI-shaped, proven) (#123) (5727792)
- Elixir endpoint SDK via a Rustler NIF (Phoenix/Plug-shaped, proven) (#122) (9f69061)
- embeddable Hop endpoint SDK prototype (receive Hop messages in a Node app) (#120) (c27a522)
- cleaned wordmark — refresh site logo + OG card (36f2626)
- icon set — the Hop bug (cursive h → route arrow → node) (9763c2c)
- brand identity refresh — cursive mark, Poppins, icons + OG (fa22fab)
- carry L2CAP PSM in the BLE advert; gentle bursty Android central (91031b3)
- tap-to-retry a 'Not sent' message (ab79ce4)
- sticky 'direct' — keep a once-direct peer in Nearby while reachable (16383cd)
- persistence story (deferred sends, invites, channel threads, re-advertise) + direct-by-link categorization + multi-peer BLE (64dd672)
- direct/mesh peer sections + per-peer link-type indicators (iOS parity) (c0ca0e0)
- Channels stack nav parity (access modes, invites, browse, revocation) (663ac21)
- Channels stack nav + access modes, invites, browse, revocation (1a198ae)
- expose hps access modes, invites, discovery, reach, revocation + app secret (f25d3af)
- hps access modes, invites, discovery, reach, revocation (steps 2-3) (01ce403)
- app-secret key material + directory app-scoping (hps isolation step 1) (a6cb45e)
- persist chat history, unread app badge, not-sent state, relay pinning (2540341)
- multipart messages — text + multiple images as one payload (both platforms) (68ae8ac)
- hdp/hops/HNS architecture — hops:// wiring + hop-endpoint + DESIGN (§30/§31) (b242afa)
- robust delay-tolerant ACKs + DTN-scale lifetimes & retx backoff (§7) (4a3153f)
- forward-before-evict custody policy + bigger cloud relay window (§6) (375a133)
- transparent carrier-streaming — any message/service auto-chunks (§20) (e1f3f99)
- drop open-web HTTP fetch; document hops:// origin-gateway model (9753820)
- add a contact to the address book by base58 address (e2f3af1)
- handoff logs show msg id+dest; app surfaces messages from unknown senders (ecbf668)
- clear-relay-queue button + Node.clear_queue / FFI (5ace3bc)
- live network-log stream over HTTP (§ops) (b4817a7)
- identify-resolved traces, relays in list, per-link symbols (§29) (7677739)
- service calls (hop.identify) + per-region subdomains phase 2 (§29) (d812ccd)
- per-region subdomains (phase 1) — wildcard cert, host routing, dual-stack (7128fd7)
- cross-partition handoff — the offline-destination mailbox (§28) (6dfce38)
- device check-in — auto-connect + auto-reconnect to relay.hopme.sh (3aa1b09)
- per-region identity + wire --region/--advertise (§28) (d53b875)
- surface traces+routes in app; stamp carrier app id per hop (f2f5c76)
- §28 liveness registry + pull-on-wake dialing (ec90631)
- §27 provenance traces + learned routes + utility-prioritized epidemic (5ccd1fc)

### Other
- bootstrap script to create + publish the SDK mirror repos (#163) (fefab3e)
- make the TLS-served reach record the only name path (drop DNSSEC-over-DoH) (#139) (549e4fc)
- one consistent endpoint surface across node/python/go/elixir (#125) (f974dad)
- gate HopDriver coverage from llvm-cov JSON, not a positional awk column (F-CI-7) (#102) (a0c9489)
- close 5 guard gaps pass-18 found (anchor-job bypass + 4 dash/matrix holes) (#98) (3a402da)
- verify_publish uses verify_strict, matching the crate-wide ed25519 convention (#95) (c4fc5be)
- migrate to Astro 7 (land Dependabot #23) (#88) (f3ca267)
- lift HopBearer+Hns.swift 59% -> 99% + add a per-file coverage floor (#83) (a166f1d)
- prevent_destroy on the anycast IPs + relay-identity secret (2nd-pass finding) (#80) (ad52485)
- decompose the 1489-line HopBearer god-object into per-concern collaborators (C+ → A) (#76) (c032300)
- decompose the 1895-line HopBearer god-object into per-concern collaborators (B- → A) (#75) (1a3f3d7)
- route dedup through the pure keep-rule cores; fix inverted Android dedup-ordering docs (#72) (d8174fd)
- close the two capping defects in Endpoint and the Kotlin SDK (#74) (847e565)
- root-cause the recurring 0-byte LICENSE bug; version floors; guard (all → A) (#73) (cd39c36)
- strip em-dashes from this session's Apple coverage test files (#67) (f3a12f0)
- canonicalize the delivery-vaccine shape at verify (r15-01) — close the is_ack twin on the vaccine id (#56) (e86d44d)
- bind the entire private inner into the wire id (r14-01) — close the flags.request_ack twin residual (#55) (52363ab)
- rebuild wasm pkg for wire v5 (r13 header-bound private id) (#54) (8984e04)
- bind the §39 recognition header into the private wire id (r13-01) — close the 7th chimera vector at its root (#52) (7c136b4)
- cargo fmt the r12 chimera test (rustfmt line-wrap) (#51) (0ad7f07)
- close the §39 recognition-header chimera (r12-01) — the 6th vector (#50) (a322559)
- authorize the response-cleanup purges (r11-01) - close the last purge vector (#47) (fb42a97)
- enforce the §39 private-bundle invariant at the gate (r10-01 root fix) (#46) (2c8f3b5)
- close the traced-ACK forgery on the DEFAULT private send path (r9-01) (#45) (f460972)
- close the private-chimera bypass of the traced-ACK authorization (r8-01) (#44) (de9c5a8)
- restore LICENSE.md (FSL-1.1-ALv2) - the reaudit-close restore did not land (#35) (2484fce)
- fix Android dial-backoff wedge caught by the hardware gauntlet (#16) (7ff7ec4)
- fix the relay-teardown destroy cycle by count-gating the whole https chain (a7dd21f)
- gate relay observability on relays_enabled + grant build SA logging.admin (adfb0db)
- take the relay fleet OFFLINE (relays_enabled=false) for the P2P test phase (a1c8f96)
- remove dead Spacelift bootstrap + tighten cloudbuild SA off roles/owner (4299d13)
- supply fleet vars as TF_VAR_* env on the apply step (e5b898c)
- migrate GitOps off Spacelift → GCS state + Cloud Build (OpenTofu) (09692d6)
- refresh GATT cache on dial timeout to break the discovery wedge (bbe1483)
- relay-queue label → "broadcast", fix iOS endpoints misclassification + Android jumbled service log (7d9ae6a)
- remove Destination::InternetEgress (mesh-visible internet-bound leak) (e99eb3c)
- app identifiers + code packages net.waldrip.* / co.hopmesh.* -> sh.hopme.* (d5decd9)
- JNA compileOnly to avoid jar+aar duplicate-class clash (497f478)
- fix the stale F0900BEA beacon UUID -> 7ED7BEAC in the Android driver (1404aaf)
- split into HopContract (no libhop) + Hop (libhop node) — unblocks the app cutover (68bf3ce)
- thin HopDriver composing the SDK + all four bearers (mirror of Apple) (58822a5)
- re-home all four bearers as independent modules on the Kotlin SDK (adba3e6)
- ship libhop as an xcframework binary target (device-buildable stack) (470c309)
- thin HopDriver composing the SDK + all four bearers (5977496)
- rename the cdylib/staticlib hop_ffi -> hop (the C ABI is `libhop`) (0b2b098)
- rename sdk/wrappers/swift -> sdk/wrappers/Hop (clean SwiftPM package id) (e376d96)
- contract-purity guardrail — no transport symbols in the contract/SDK (b4cac24)
- bearer kit at parity with Swift (Bearer/LinkSink/BearerManager/HopRuntime) (83e22bf)
- a full Hop client in pure C over hop.h (eacd332)
- idiomatic Kotlin/JVM wrapper over hop.h (JNA) + smoke (13c3db2)
- add the hops:// request/response surface to the C ABI (f5e1791)
- RuntimeSmoke — prove HopRuntime + a Bearer drive the node end to end (a37c34b)
- re-home all four bearers as independent packages on the Hop SDK (1b59005)
- ship the bearer kit with Hop (Bearer/LinkSink, BearerManager, HopRuntime) (ada77f1)
- idiomatic Swift wrapper over hop.h (Hop) + macOS smoke (5288ecb)
- expand the C ABI into a complete client SDK (37e7965)
- a cbindgen C ABI for hop-core — the universal client + bearer contract (d2a93aa)
- emit a timestamp `expireAt` so the TTL policy actually sweeps (aaa9132)
- add `relaymac` — a headless RELAY-ONLY macOS test client (ebd5328)
- request an OAuth scope for the metadata-server token (fix relay 401) (bad27a3)
- add Role.relayOnly — relay and nothing else (no BLE/LAN/Wi-Fi) (8951163)
- guard UNUserNotificationCenter for iOS only (macOS CLI crash) (4a562bf)
- make untraceable messaging the DEFAULT + private delivery-ACK (c548a45)
- tag shared-bearer peers by real transport in the prod UI (41194c8)
- cloud relay as its own lib (both platforms) + re-enable the relay (774c17e)
- Wi-Fi Direct as its own lib + register in clean-room & prod (c81bfee)
- add .gitignore, untrack 1074 committed build artifacts (7796877)
- fold the shared HopBearers libs into the prod path (App→hop core→bearers) (b23aac1)
- BearerManager(baseLinkId:) to offset the global link-id space (fabbfb0)
- LAN bearer as its own lib (Bonjour + TCP) + thread-safe manager (9ece2c8)
- split into core + one lib per bearer (no master library) (f6dfc10)
- add .gitignore, untrack committed build/.gradle artifacts (324918f)
- README — the one-interface contract + how to write/register a bearer (a3e70f9)
- deterministic registry tests (radio-free proof of multiplexing) (9902d65)
- transport-agnostic Bearer registry (BearerManager) + BLE extraction (3733ea6)
- shared bearer-library keystone — LinkSink/Bearer contract (30debfd)
- NSLog alongside print so the iOS device log is capturable for L2CAP-teardown diagnosis (a040b92)
- fix BLE redial storm (prefix-less peers dialed every advert) + bug-hunt findings (9ebb3c3)
- clean-room validation results + 4-device harness config (fc05887)
- fix multi-peer flood + securing/delivery; P2P test mode + harness (8a613c2)
- stop the re-gossip directory re-flood (multi-peer resource exhaustion fix) (22530a9)
- prove duplicate-link handling is sound; add BLE handshake-flow diagnostics (8c8d65f)
- pure-L2CAP with advert-carried PSM + platform-biased roles (Ditto pattern) (ebb577d)
- one-pipe-per-pair gate, immediate fallback, skip L2CAP to Android (b5295ed)
- always discover GATT services so the GATT-data fallback can find DATA (bd638d9)
- flush deferred ("Securing") content when a session forms, not just on tick (11039bc)
- auto-heal a wedged BLE stack when Bluetooth cycles (53f2796)
- re-gossip adverts over stable links so prekeys propagate without a reconnect (a2e1320)
- keyboard-open layout + offline label (0a91802)
- fix frozen keyboard — drive SendingIndicator pulse from the 1s tick (2dc0c69)
- broadcast iBeacon to wake backgrounded/killed iPhones (cross-platform wake) (d05e665)
- GATT-first by default, Android-only filter, 0xFFFF-mfg PSM guard, backoff reconnect (c60593d)
- run BLE pure-peripheral — central scan was starving discovery (f386fd5)
- require ratcheting for device-to-device content (fix lockless sends) (d3f6448)
- diagnostics parity — service log, relay queue, hop traces, identify (dfb2a03)
- unified 4-tab information architecture across iOS + Android (488c5b7)
- hops:// WebView browser over the mesh — full iOS parity (80d1bd9)
- hps:// pub/sub, hops:// fetch, HNS — feature parity with iOS (8df1421)
- image send + display (feature parity, step 1) (36e8809)
- auto-connect + auto-reconnect the cloud relay (delivery parity) (66b4f60)
- // pub/sub phase 3: FFI surface + iOS channels/services UI (e0cfd19)
- regenerated Info.plist — BGProcessing task id permitted (82a9bfc)
- reconnect the relay on background wake + BGProcessing backlog drain (9b3180d)
- online-only relay-to-relay epidemic (gated, off by default) — §28 phase 1 (d1a5fb0)
- keep relay custody of request_ack bundles until the delivery ACK (d0c7db4)
- persist carrier reassembly so large transfers survive background wakes (7e27732)
- track chunked-message delivery status under the original id (857d2a1)
- add HopWebView.swift to project.yml sources (f553808)
- SessionReset — recipient heals a desynced ratchet (6c9f50e)
- persist forward-secret ratchet sessions across restarts (921a931)
- link-layer fragmentation for oversized records — fix silent drop (81c901c)
- // pub/sub phase 2: node wire flow over Broadcast (8f78622)
- hps:// pub/sub crypto primitives — phase 1 (DESIGN §32) (81c8886)
- DNSSEC ECDSA P-256 (alg 13) verification (aa76eae)
- carry the DNSSEC proof in HnsAnswer — trustless multi-hop (phase 3) (193b17f)
- enforce DNSSEC end-to-end — host fetches chain, core validates (phase 2c + 4) (1e3a336)
- validate_and_extract — one-shot trustless resolution (phase 2c) (a12a80b)
- DNSSEC chain assembler from DoH responses — phase 2b (469a020)
- DoH JSON parsing — phase 2a (16d7491)
- DNSSEC chain walk to the root anchor — phase 1b (8e963aa)
- DNSSEC validation primitives (RSA/SHA-256) — phase 1 (604f656)
- decentralized multi-hop DNSSEC HNS + §32 hps:// pub/sub (809ac54)
- delay-tolerant resolution + utf-8 error bodies (5832910)
- fix dial_relay deadlock (non-blocking socket) (287a641)
- a no-internet node resolves by broadcasting to connected peers (822e00e)
- prune expired records so they leave the device (80501d9)
- routable leaf on the mesh + app labels endpoint by its domain (ab43f0f)
- list hops:// endpoints separately, not as relays (4973785)
- render scheme-honestly (hops:// vs https://) (e69a077)
- // WebView + content-type plumbing (27b04ef)
- dial the hops:// endpoint directly so requests actually deliver (a3a70f1)
- also serve standard HTTPS (reverse-proxy to origin) (3714c7c)
- rename build SA to hop-cloudbuild (avoid soft-delete collision with probe) (3b3628e)
- build SA for the Cloud Build trigger (fresh project has no legacy CB SA) (66cf37f)
- only infra relays stamp a trace app id; devices stay fabric (ac3ab06)
- 2nd-gen Cloud Build GitHub connection for GitOps trigger (e8420d9)
- GitOps build trigger (GitHub App, gated) + sha-tracking deploy + region allowlist (9aa4b92)
- deploy Cloud Run relay to all available regions (f9c90e5)
- drop timeout_sec on serverless-NEG backend (unsupported) (2cf653b)
- enable global LB anycast + relay.hopme.sh A record (8e412cd)
- create Cloud DNS public zone for hopme.sh (384dee0)
- remove one-time import blocks (resources now in Spacelift state) (b3598b7)
- fix cloud run IAM-member import ID (space-separated) (805b60b)
- fix IAM-member import IDs (space-separated form) (4887e75)
- temporary import blocks to adopt live resources into Spacelift state (b860e52)
- stop tracking the Spacelift credential config (32b958c)
- Spacelift↔GCP OIDC bootstrap (2d356ea)

### Performance
- BLE scan BALANCED not LOW_LATENCY — free radio time for the peripheral role (a91cbeb)

### Refactor
- enforce purpose/platform/package (collapse sdk/wrappers, apps/web -> apps/web/site) (#116) (ffe08fe)

### Testing
- kill the unseeded stadium delivery-gate flake (seed-retry fallback) (#140) (5255942)
- close store.rs and hps.rs coverage gaps to 100 percent (#81) (8087fa5)
- extract DemoFormat + 21 tests, testable surface 0% → 100% + CI gate (#71) (34cd949)
- extract HopDemo pure logic into HopDemoKit + 100% headless coverage gate (#70) (6d45af6)
- seam refactor takes BleBearer 7% → 97% (CB-free cores), replace shadow tests (#69) (41fe27a)
- LAN 95% / Relay 100% / BLE 99% + fix non-restartable LanBearer (#68) (4609db2)
- raise host-testable coverage to A (99%) by splitting off the wasm-only surface (#66) (f71cb7e)
- Robolectric suite takes the driver from D (~16%) to A (94% line) (#65) (8cf43d7)
- headless-node suite raises HopBearer.swift 12.8% -> 88.5% (F -> A) + CI coverage floor (#64) (ee4c245)
- real loopback integration tests for LAN + Relay bearers to >=80% coverage, CI gating, compile-bug root cause (#63) (917c617)
- raise hop-relayd line coverage 41.2% -> 81.1% (#62) (b3100ea)
- cover the JNA surface against real libhop to 99.5% (F → A) + CI coverage gate (#60) (1d4fe35)
- raise line coverage 39.4% -> 80.9% (#61) (2a7ad21)
- cover the FFI/UniFFI HopNode surface + free helpers (crate 56.5% -> 96%) (#59) (72740a3)
- cover the REST layer to 96.7% (from 40.5%) (#58) (885f89b)
- refresh fleet address map (sh.hopme.demo identities) (a8230a9)
- space Android deep-link sends to avoid LAUNCH_MULTIPLE drops (f375895)
- re-add bush + Decklan's iPad to the 6-device fleet (1e5ed2b)
- add bush + ipad to the address map; vscode hopmac debug config (40cd72a)
- exchange prekeys in ffi + ws round-trip tests (require-ratchet) (58d0e44)
- out-of-order SessionMessages decrypt through the node accessor (f272ae3)
- verify fully hands-off autodeploy (push only) (5bc9d82)
- trivial change to exercise autodeploy + before_apply hook (de095c2)

