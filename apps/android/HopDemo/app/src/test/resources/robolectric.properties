# cov/android-demo: pin the Robolectric SDK image so the JVM demo-app suite runs against a single,
# widely-cached android-all jar (SDK 33) rather than whatever compileSdk happens to be. minSdk is 29 and
# none of the exercised helpers need a newer framework. (Same pin the driver suite uses.)
sdk=33
