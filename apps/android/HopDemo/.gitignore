# Android / Gradle build outputs, regenerated by the build, never commit.
.gradle/
**/build/
/local.properties
*.iml
.idea/
.DS_Store
/captures
.externalNativeBuild
.cxx
# NOTE: generated/ (UniFFI bindings + jniLibs .so) is produced by ../build-aar.sh
# and is ignored at the repo root; run that script before building a clean checkout.
